modeltype Ecore uses ecore('http://www.eclipse.org/emf/2002/Ecore');

transformation DuplicateModel(in source: Ecore, out target: Ecore);

main() {
	source.rootObjects()[EPackage] -> map copyPackage();
}

mapping EPackage::copyPackage():EPackage {
	init {
		result := self.deepclone()
	}
}