# Java Bindings for SysML2

In this simple example, we show how to generate the SysML2 Java bindings, given the SysML2 LionWeb Language definiton.

As part of the example we simply create a few nodes.

## Future Development

In the future, we plan to define a few partitions and save them as part of a LionWeb Archive.