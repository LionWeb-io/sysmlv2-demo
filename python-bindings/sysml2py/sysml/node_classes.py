from abc import ABC
from dataclasses import dataclass
from enum import Enum
from typing import Optional, cast, List
from lionweb.model.classifier_instance_utils import get_only_reference_value_by_reference_name, get_property_value_by_name
from lionweb.model.impl.dynamic_node import DynamicNode
from lionweb.model.reference_value import ReferenceValue
from lionweb.model import Node
from .language import get_language, get_subclassification, get_specialization, get_owning_membership, get_membership, get_documentation, get_comment, get_annotation, get_textual_representation, get_feature_membership, get_redefinition, get_subsetting, get_feature_typing, get_type_featuring, get_feature_inverting, get_feature_chaining, get_reference_subsetting, get_cross_subsetting, get_conjugation, get_multiplicity, get_intersecting, get_unioning, get_disjoining, get_differencing, get_end_feature_membership, get_result_expression_membership, get_return_parameter_membership, get_parameter_membership, get_multiplicity_range, get_feature_value, get_metadata_feature, get_item_flow_end, get_item_feature, get_element_filter_membership, get_package, get_library_package, get_feature_reference_expression, get_metadata_access_expression, get_null_expression, get_index_expression, get_operator_expression, get_invocation_expression, get_collect_expression, get_literal_infinity, get_literal_expression, get_literal_integer, get_select_expression, get_literal_rational, get_literal_boolean, get_literal_string, get_feature_chain_expression, get_dependency, get_namespace_import, get_membership_import, get_interface_usage, get_connection_usage, get_connector_as_usage, get_variant_membership, get_definition, get_reference_usage, get_attribute_usage, get_enumeration_usage, get_enumeration_definition, get_attribute_definition, get_occurrence_definition, get_life_class, get_part_definition, get_item_definition, get_port_usage, get_port_definition, get_conjugated_port_definition, get_port_conjugation, get_flow_connection_usage, get_allocation_usage, get_allocation_definition, get_connection_definition, get_state_usage, get_transition_usage, get_accept_action_usage, get_calculation_usage, get_requirement_usage, get_requirement_definition, get_constraint_definition, get_concern_usage, get_concern_definition, get_case_usage, get_case_definition, get_calculation_definition, get_action_definition, get_analysis_case_usage, get_analysis_case_definition, get_verification_case_usage, get_verification_case_definition, get_use_case_usage, get_use_case_definition, get_view_usage, get_view_definition, get_viewpoint_usage, get_viewpoint_definition, get_rendering_usage, get_rendering_definition, get_metadata_usage, get_interface_definition, get_conjugated_port_typing, get_transition_feature_membership, get_exhibit_state_usage, get_state_subaction_membership, get_state_definition, get_succession_flow_connection_usage, get_flow_connection_definition, get_requirement_verification_membership, get_requirement_constraint_membership, get_include_use_case_usage, get_objective_membership, get_satisfy_requirement_usage, get_subject_membership, get_stakeholder_membership, get_framed_concern_membership, get_actor_membership, get_view_rendering_membership, get_namespace_expose, get_membership_expose, get_binding_connector_as_usage, get_succession_as_usage, get_fork_node, get_control_node, get_join_node, get_send_action_usage, get_decision_node, get_merge_node, get_loop_action_usage, get_trigger_invocation_expression, get_assignment_action_usage, get_for_loop_action_usage, get_if_action_usage, get_while_loop_action_usage, get_terminate_action_usage, get_metadata_definition, get_alias_ids_container, get_text_container, get_featuring, get_relationship, get_element, get_annotating_element, get_step, get_feature, get_type, get_namespace, get_behavior, get_class, get_classifier, get_succession, get_connector, get_structure, get_part_usage, get_item_usage, get_occurrence_usage, get_usage, get_data_type, get_action_usage, get_item_flow, get_association_structure, get_association, get_predicate, get_function, get_perform_action_usage, get_event_occurrence_usage, get_succession_item_flow, get_interaction, get_assert_constraint_usage, get_constraint_usage, get_boolean_expression, get_expression, get_invariant, get_expose, get_import, get_binding_connector, get_metaclass


class VisibilityKind(Enum):
    private = 'private'
    protected = 'protected'
    public = 'public'


class FeatureDirectionKind(Enum):
    in_ = 'in'
    inout = 'inout'
    out = 'out'


class PortionKind(Enum):
    timeslice = 'timeslice'
    snapshot = 'snapshot'


class TransitionFeatureKind(Enum):
    trigger = 'trigger'
    guard = 'guard'
    effect = 'effect'


class StateSubactionKind(Enum):
    entry = 'entry'
    do = 'do'
    exit = 'exit'


class RequirementConstraintKind(Enum):
    assumption = 'assumption'
    requirement = 'requirement'


class TriggerKind(Enum):
    when = 'when'
    at = 'at'
    after = 'after'


class AliasIdsContainer(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_alias_ids_container()

    @property
    def aliasIds(self) ->str:
        return cast(str, get_property_value_by_name(self, 'aliasIds'))

    @aliasIds.setter
    def aliasIds(self, value: str):
        property_ = self.get_classifier().require_property_by_name('aliasIds')
        self.set_property_value(property=property_, value=value)


class IElement(Node, ABC):
    pass


class IRelationship(Node, ABC):
    pass


class Specialization(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_specialization()

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def general(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'general')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @general.setter
    def general(self, general: 'IType'):
        reference = self.get_classifier().get_reference_by_name('general')
        if self.general:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(general, general
            .name))

    @property
    def specific(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'specific')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @specific.setter
    def specific(self, specific: 'IType'):
        reference = self.get_classifier().get_reference_by_name('specific')
        if self.specific:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(specific,
            specific.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Subclassification(Specialization):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_subclassification()

    @property
    def superclassifier(self) ->Optional['IClassifier']:
        res = get_only_reference_value_by_reference_name(self,
            'superclassifier')
        if res:
            return cast(IClassifier, res.referred)
        else:
            return None

    @superclassifier.setter
    def superclassifier(self, superclassifier: 'IClassifier'):
        reference = self.get_classifier().get_reference_by_name(
            'superclassifier')
        if self.superclassifier:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(superclassifier,
            superclassifier.name))

    @property
    def owningClassifier(self) ->Optional['IClassifier']:
        res = get_only_reference_value_by_reference_name(self,
            'owningClassifier')
        if res:
            return cast(IClassifier, res.referred)
        else:
            return None

    @owningClassifier.setter
    def owningClassifier(self, owningClassifier: 'IClassifier'):
        reference = self.get_classifier().get_reference_by_name(
            'owningClassifier')
        if self.owningClassifier:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningClassifier,
            owningClassifier.name))

    @property
    def subclassifier(self) ->Optional['IClassifier']:
        res = get_only_reference_value_by_reference_name(self, 'subclassifier')
        if res:
            return cast(IClassifier, res.referred)
        else:
            return None

    @subclassifier.setter
    def subclassifier(self, subclassifier: 'IClassifier'):
        reference = self.get_classifier().get_reference_by_name('subclassifier'
            )
        if self.subclassifier:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subclassifier,
            subclassifier.name))


class Membership(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_membership()

    @property
    def memberElementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'memberElementId'))

    @memberElementId.setter
    def memberElementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'memberElementId')
        self.set_property_value(property=property_, value=value)

    @property
    def membershipOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'membershipOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @membershipOwningNamespace.setter
    def membershipOwningNamespace(self, membershipOwningNamespace: 'INamespace'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'membershipOwningNamespace')
        if self.membershipOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            membershipOwningNamespace, membershipOwningNamespace.name))

    @property
    def memberShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'memberShortName'))

    @memberShortName.setter
    def memberShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'memberShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def memberElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'memberElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @memberElement.setter
    def memberElement(self, memberElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('memberElement'
            )
        if self.memberElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(memberElement,
            memberElement.name))

    @property
    def memberName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'memberName'))

    @memberName.setter
    def memberName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('memberName'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class OwningMembership(Membership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_owning_membership()

    @property
    def ownedMemberElementId(self) ->str:
        return cast(str, get_property_value_by_name(self,
            'ownedMemberElementId'))

    @ownedMemberElementId.setter
    def ownedMemberElementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'ownedMemberElementId')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedMemberShortName(self) ->str:
        return cast(str, get_property_value_by_name(self,
            'ownedMemberShortName'))

    @ownedMemberShortName.setter
    def ownedMemberShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'ownedMemberShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedMemberName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'ownedMemberName'))

    @ownedMemberName.setter
    def ownedMemberName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'ownedMemberName')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedMemberElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedMemberElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @ownedMemberElement.setter
    def ownedMemberElement(self, ownedMemberElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedMemberElement')
        if self.ownedMemberElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedMemberElement, ownedMemberElement.name))


class INamespace(Node, ABC):
    pass


class IImport(Node, ABC):
    pass


class IAnnotatingElement(Node, ABC):
    pass


class Comment(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_comment()

    @property
    def locale(self) ->str:
        return cast(str, get_property_value_by_name(self, 'locale'))

    @locale.setter
    def locale(self, value: str):
        property_ = self.get_classifier().require_property_by_name('locale')
        self.set_property_value(property=property_, value=value)

    @property
    def body(self) ->str:
        return cast(str, get_property_value_by_name(self, 'body'))

    @body.setter
    def body(self, value: str):
        property_ = self.get_classifier().require_property_by_name('body')
        self.set_property_value(property=property_, value=value)

    @property
    def annotatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'annotatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_annotated_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotatingRelationship(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotatingRelationship')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotating_relationship(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotatingRelationship'),
            ReferenceValue(new_element, new_element.name))

    @property
    def annotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'annotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningAnnotatingRelationship(self) ->Optional['Annotation']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatingRelationship')
        if res:
            return cast(Annotation, res.referred)
        else:
            return None

    @owningAnnotatingRelationship.setter
    def owningAnnotatingRelationship(self, owningAnnotatingRelationship:
        'Annotation'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatingRelationship')
        if self.owningAnnotatingRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatingRelationship, owningAnnotatingRelationship.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Documentation(Comment):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_documentation()

    @property
    def documentedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'documentedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @documentedElement.setter
    def documentedElement(self, documentedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'documentedElement')
        if self.documentedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            documentedElement, documentedElement.name))


class Annotation(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_annotation()

    @property
    def annotatingElement(self) ->Optional['IAnnotatingElement']:
        res = get_only_reference_value_by_reference_name(self,
            'annotatingElement')
        if res:
            return cast(IAnnotatingElement, res.referred)
        else:
            return None

    @annotatingElement.setter
    def annotatingElement(self, annotatingElement: 'IAnnotatingElement'):
        reference = self.get_classifier().get_reference_by_name(
            'annotatingElement')
        if self.annotatingElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            annotatingElement, annotatingElement.name))

    @property
    def annotatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'annotatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @annotatedElement.setter
    def annotatedElement(self, annotatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'annotatedElement')
        if self.annotatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(annotatedElement,
            annotatedElement.name))

    @property
    def owningAnnotatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningAnnotatedElement.setter
    def owningAnnotatedElement(self, owningAnnotatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatedElement')
        if self.owningAnnotatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatedElement, owningAnnotatedElement.name))

    @property
    def ownedAnnotatingElement(self) ->Optional['IAnnotatingElement']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedAnnotatingElement')
        if res:
            return cast(IAnnotatingElement, res.referred)
        else:
            return None

    @ownedAnnotatingElement.setter
    def ownedAnnotatingElement(self, ownedAnnotatingElement:
        'IAnnotatingElement'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedAnnotatingElement')
        if self.ownedAnnotatingElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedAnnotatingElement, ownedAnnotatingElement.name))

    @property
    def owningAnnotatingElement(self) ->Optional['IAnnotatingElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatingElement')
        if res:
            return cast(IAnnotatingElement, res.referred)
        else:
            return None

    @owningAnnotatingElement.setter
    def owningAnnotatingElement(self, owningAnnotatingElement:
        'IAnnotatingElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatingElement')
        if self.owningAnnotatingElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatingElement, owningAnnotatingElement.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class TextualRepresentation(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_textual_representation()

    @property
    def language(self) ->str:
        return cast(str, get_property_value_by_name(self, 'language'))

    @language.setter
    def language(self, value: str):
        property_ = self.get_classifier().require_property_by_name('language')
        self.set_property_value(property=property_, value=value)

    @property
    def body(self) ->str:
        return cast(str, get_property_value_by_name(self, 'body'))

    @body.setter
    def body(self, value: str):
        property_ = self.get_classifier().require_property_by_name('body')
        self.set_property_value(property=property_, value=value)

    @property
    def representedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'representedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @representedElement.setter
    def representedElement(self, representedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'representedElement')
        if self.representedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            representedElement, representedElement.name))

    @property
    def annotatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'annotatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_annotated_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotatingRelationship(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotatingRelationship')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotating_relationship(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotatingRelationship'),
            ReferenceValue(new_element, new_element.name))

    @property
    def annotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'annotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningAnnotatingRelationship(self) ->Optional['Annotation']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatingRelationship')
        if res:
            return cast(Annotation, res.referred)
        else:
            return None

    @owningAnnotatingRelationship.setter
    def owningAnnotatingRelationship(self, owningAnnotatingRelationship:
        'Annotation'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatingRelationship')
        if self.owningAnnotatingRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatingRelationship, owningAnnotatingRelationship.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IType(Node, ABC):
    pass


class IFeaturing(Node, ABC):
    pass


class FeatureMembership(OwningMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_membership()

    @property
    def ownedMemberFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedMemberFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @ownedMemberFeature.setter
    def ownedMemberFeature(self, ownedMemberFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedMemberFeature')
        if self.ownedMemberFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedMemberFeature, ownedMemberFeature.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def type(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'type')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @type.setter
    def type(self, type: 'IType'):
        reference = self.get_classifier().get_reference_by_name('type')
        if self.type:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(type, type.name))

    @property
    def feature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'feature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @feature.setter
    def feature(self, feature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('feature')
        if self.feature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(feature, feature
            .name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IFeature(Node, ABC):
    pass


class Subsetting(Specialization):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_subsetting()

    @property
    def subsettedFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'subsettedFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @subsettedFeature.setter
    def subsettedFeature(self, subsettedFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'subsettedFeature')
        if self.subsettedFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subsettedFeature,
            subsettedFeature.name))

    @property
    def subsettingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'subsettingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @subsettingFeature.setter
    def subsettingFeature(self, subsettingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'subsettingFeature')
        if self.subsettingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            subsettingFeature, subsettingFeature.name))

    @property
    def owningFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'owningFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @owningFeature.setter
    def owningFeature(self, owningFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('owningFeature'
            )
        if self.owningFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningFeature,
            owningFeature.name))


class Redefinition(Subsetting):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_redefinition()

    @property
    def redefiningFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'redefiningFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @redefiningFeature.setter
    def redefiningFeature(self, redefiningFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'redefiningFeature')
        if self.redefiningFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            redefiningFeature, redefiningFeature.name))

    @property
    def redefinedFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'redefinedFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @redefinedFeature.setter
    def redefinedFeature(self, redefinedFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'redefinedFeature')
        if self.redefinedFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(redefinedFeature,
            redefinedFeature.name))


class FeatureTyping(Specialization):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_typing()

    @property
    def typedFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'typedFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @typedFeature.setter
    def typedFeature(self, typedFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('typedFeature')
        if self.typedFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(typedFeature,
            typedFeature.name))

    @property
    def type(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'type')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @type.setter
    def type(self, type: 'IType'):
        reference = self.get_classifier().get_reference_by_name('type')
        if self.type:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(type, type.name))

    @property
    def owningFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'owningFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @owningFeature.setter
    def owningFeature(self, owningFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('owningFeature'
            )
        if self.owningFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningFeature,
            owningFeature.name))


class TypeFeaturing(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_type_featuring()

    @property
    def featureOfType(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureOfType')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureOfType.setter
    def featureOfType(self, featureOfType: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureOfType'
            )
        if self.featureOfType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureOfType,
            featureOfType.name))

    @property
    def featuringType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'featuringType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @featuringType.setter
    def featuringType(self, featuringType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('featuringType'
            )
        if self.featuringType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featuringType,
            featuringType.name))

    @property
    def owningFeatureOfType(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureOfType')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @owningFeatureOfType.setter
    def owningFeatureOfType(self, owningFeatureOfType: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureOfType')
        if self.owningFeatureOfType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureOfType, owningFeatureOfType.name))

    @property
    def type(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'type')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @type.setter
    def type(self, type: 'IType'):
        reference = self.get_classifier().get_reference_by_name('type')
        if self.type:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(type, type.name))

    @property
    def feature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'feature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @feature.setter
    def feature(self, feature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('feature')
        if self.feature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(feature, feature
            .name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class FeatureInverting(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_inverting()

    @property
    def featureInverted(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'featureInverted')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureInverted.setter
    def featureInverted(self, featureInverted: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'featureInverted')
        if self.featureInverted:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureInverted,
            featureInverted.name))

    @property
    def invertingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'invertingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @invertingFeature.setter
    def invertingFeature(self, invertingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'invertingFeature')
        if self.invertingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(invertingFeature,
            invertingFeature.name))

    @property
    def owningFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'owningFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @owningFeature.setter
    def owningFeature(self, owningFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('owningFeature'
            )
        if self.owningFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningFeature,
            owningFeature.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class FeatureChaining(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_chaining()

    @property
    def chainingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'chainingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @chainingFeature.setter
    def chainingFeature(self, chainingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'chainingFeature')
        if self.chainingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(chainingFeature,
            chainingFeature.name))

    @property
    def featureChained(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureChained'
            )
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureChained.setter
    def featureChained(self, featureChained: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'featureChained')
        if self.featureChained:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureChained,
            featureChained.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ReferenceSubsetting(Subsetting):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_reference_subsetting()

    @property
    def referencedFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'referencedFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @referencedFeature.setter
    def referencedFeature(self, referencedFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'referencedFeature')
        if self.referencedFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencedFeature, referencedFeature.name))

    @property
    def referencingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'referencingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @referencingFeature.setter
    def referencingFeature(self, referencingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'referencingFeature')
        if self.referencingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencingFeature, referencingFeature.name))


class CrossSubsetting(Subsetting):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_cross_subsetting()

    @property
    def crossedFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossedFeature'
            )
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossedFeature.setter
    def crossedFeature(self, crossedFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'crossedFeature')
        if self.crossedFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossedFeature,
            crossedFeature.name))

    @property
    def crossingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'crossingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossingFeature.setter
    def crossingFeature(self, crossingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'crossingFeature')
        if self.crossingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossingFeature,
            crossingFeature.name))


class Conjugation(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_conjugation()

    @property
    def originalType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'originalType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @originalType.setter
    def originalType(self, originalType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('originalType')
        if self.originalType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(originalType,
            originalType.name))

    @property
    def conjugatedType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'conjugatedType'
            )
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @conjugatedType.setter
    def conjugatedType(self, conjugatedType: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'conjugatedType')
        if self.conjugatedType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(conjugatedType,
            conjugatedType.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Multiplicity(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_multiplicity()

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Intersecting(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_intersecting()

    @property
    def intersectingType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self,
            'intersectingType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @intersectingType.setter
    def intersectingType(self, intersectingType: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'intersectingType')
        if self.intersectingType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(intersectingType,
            intersectingType.name))

    @property
    def typeIntersected(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self,
            'typeIntersected')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @typeIntersected.setter
    def typeIntersected(self, typeIntersected: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'typeIntersected')
        if self.typeIntersected:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(typeIntersected,
            typeIntersected.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Unioning(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_unioning()

    @property
    def unioningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'unioningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @unioningType.setter
    def unioningType(self, unioningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('unioningType')
        if self.unioningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(unioningType,
            unioningType.name))

    @property
    def typeUnioned(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'typeUnioned')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @typeUnioned.setter
    def typeUnioned(self, typeUnioned: 'IType'):
        reference = self.get_classifier().get_reference_by_name('typeUnioned')
        if self.typeUnioned:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(typeUnioned,
            typeUnioned.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Disjoining(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_disjoining()

    @property
    def typeDisjoined(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'typeDisjoined')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @typeDisjoined.setter
    def typeDisjoined(self, typeDisjoined: 'IType'):
        reference = self.get_classifier().get_reference_by_name('typeDisjoined'
            )
        if self.typeDisjoined:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(typeDisjoined,
            typeDisjoined.name))

    @property
    def disjoiningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'disjoiningType'
            )
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @disjoiningType.setter
    def disjoiningType(self, disjoiningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'disjoiningType')
        if self.disjoiningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(disjoiningType,
            disjoiningType.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Differencing(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_differencing()

    @property
    def differencingType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self,
            'differencingType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @differencingType.setter
    def differencingType(self, differencingType: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'differencingType')
        if self.differencingType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(differencingType,
            differencingType.name))

    @property
    def typeDifferenced(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self,
            'typeDifferenced')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @typeDifferenced.setter
    def typeDifferenced(self, typeDifferenced: 'IType'):
        reference = self.get_classifier().get_reference_by_name(
            'typeDifferenced')
        if self.typeDifferenced:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(typeDifferenced,
            typeDifferenced.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IClassifier(Node, ABC):
    pass


class EndFeatureMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_end_feature_membership()


class IStep(Node, ABC):
    pass


class IExpression(Node, ABC):
    pass


class IClass(Node, ABC):
    pass


class IBehavior(Node, ABC):
    pass


class IFunction(Node, ABC):
    pass


class ResultExpressionMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_result_expression_membership()

    @property
    def ownedResultExpression(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedResultExpression')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @ownedResultExpression.setter
    def ownedResultExpression(self, ownedResultExpression: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedResultExpression')
        if self.ownedResultExpression:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedResultExpression, ownedResultExpression.name))


class IBooleanExpression(Node, ABC):
    pass


class IInvariant(Node, ABC):
    pass


class IPredicate(Node, ABC):
    pass


class ParameterMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_parameter_membership()

    @property
    def ownedMemberParameter(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedMemberParameter')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @ownedMemberParameter.setter
    def ownedMemberParameter(self, ownedMemberParameter: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedMemberParameter')
        if self.ownedMemberParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedMemberParameter, ownedMemberParameter.name))


class ReturnParameterMembership(ParameterMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_return_parameter_membership()


class MultiplicityRange(Multiplicity):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_multiplicity_range()

    @property
    def lowerBound(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'lowerBound')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @lowerBound.setter
    def lowerBound(self, lowerBound: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('lowerBound')
        if self.lowerBound:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(lowerBound,
            lowerBound.name))

    @property
    def upperBound(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'upperBound')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @upperBound.setter
    def upperBound(self, upperBound: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('upperBound')
        if self.upperBound:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(upperBound,
            upperBound.name))

    @property
    def bound(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'bound')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_bound(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('bound'), ReferenceValue(new_element,
            new_element.name))


class IStructure(Node, ABC):
    pass


class FeatureValue(OwningMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_value()

    @property
    def featureWithValue(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'featureWithValue')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureWithValue.setter
    def featureWithValue(self, featureWithValue: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'featureWithValue')
        if self.featureWithValue:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureWithValue,
            featureWithValue.name))

    @property
    def value(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'value')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @value.setter
    def value(self, value: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('value')
        if self.value:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(value, value.name))

    @property
    def isInitial(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isInitial'))

    @isInitial.setter
    def isInitial(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isInitial')
        self.set_property_value(property=property_, value=value)

    @property
    def isDefault(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDefault'))

    @isDefault.setter
    def isDefault(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDefault')
        self.set_property_value(property=property_, value=value)


class IMetaclass(Node, ABC):
    pass


class MetadataFeature(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_metadata_feature()

    @property
    def metaclass(self) ->Optional['IMetaclass']:
        res = get_only_reference_value_by_reference_name(self, 'metaclass')
        if res:
            return cast(IMetaclass, res.referred)
        else:
            return None

    @metaclass.setter
    def metaclass(self, metaclass: 'IMetaclass'):
        reference = self.get_classifier().get_reference_by_name('metaclass')
        if self.metaclass:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(metaclass,
            metaclass.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def annotatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'annotatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_annotated_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotatingRelationship(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotatingRelationship')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotating_relationship(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotatingRelationship'),
            ReferenceValue(new_element, new_element.name))

    @property
    def annotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'annotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningAnnotatingRelationship(self) ->Optional['Annotation']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatingRelationship')
        if res:
            return cast(Annotation, res.referred)
        else:
            return None

    @owningAnnotatingRelationship.setter
    def owningAnnotatingRelationship(self, owningAnnotatingRelationship:
        'Annotation'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatingRelationship')
        if self.owningAnnotatingRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatingRelationship, owningAnnotatingRelationship.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class IConnector(Node, ABC):
    pass


class IItemFlow(Node, ABC):
    pass


class IAssociation(Node, ABC):
    pass


class ItemFlowEnd(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_item_flow_end()

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ItemFeature(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_item_feature()

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IInteraction(Node, ABC):
    pass


class ISuccession(Node, ABC):
    pass


class ISuccessionItemFlow(Node, ABC):
    pass


class ElementFilterMembership(OwningMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_element_filter_membership()

    @property
    def condition(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'condition')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @condition.setter
    def condition(self, condition: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('condition')
        if self.condition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(condition,
            condition.name))


class Package(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_package()

    @property
    def filterCondition(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'filterCondition')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_filter_condition(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('filterCondition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class LibraryPackage(Package):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_library_package()

    @property
    def isStandard(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isStandard'))

    @isStandard.setter
    def isStandard(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isStandard'
            )
        self.set_property_value(property=property_, value=value)


class IDataType(Node, ABC):
    pass


class FeatureReferenceExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_reference_expression()

    @property
    def referent(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'referent')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @referent.setter
    def referent(self, referent: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('referent')
        if self.referent:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(referent,
            referent.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class MetadataAccessExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_metadata_access_expression()

    @property
    def referencedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'referencedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @referencedElement.setter
    def referencedElement(self, referencedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'referencedElement')
        if self.referencedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencedElement, referencedElement.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class NullExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_null_expression()

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class InvocationExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_invocation_expression()

    @property
    def argument(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'argument')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_argument(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('argument'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class OperatorExpression(InvocationExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_operator_expression()

    @property
    def operator(self) ->str:
        return cast(str, get_property_value_by_name(self, 'operator'))

    @operator.setter
    def operator(self, value: str):
        property_ = self.get_classifier().require_property_by_name('operator')
        self.set_property_value(property=property_, value=value)


class IndexExpression(OperatorExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_index_expression()


class CollectExpression(OperatorExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_collect_expression()


class LiteralExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_expression()

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class LiteralInfinity(LiteralExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_infinity()


class LiteralInteger(LiteralExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_integer()

    @property
    def value(self) ->int:
        return cast(int, get_property_value_by_name(self, 'value'))

    @value.setter
    def value(self, value: int):
        property_ = self.get_classifier().require_property_by_name('value')
        self.set_property_value(property=property_, value=value)


class SelectExpression(OperatorExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_select_expression()


class LiteralRational(LiteralExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_rational()

    @property
    def value(self) ->float:
        return cast(float, get_property_value_by_name(self, 'value'))

    @value.setter
    def value(self, value: float):
        property_ = self.get_classifier().require_property_by_name('value')
        self.set_property_value(property=property_, value=value)


class LiteralBoolean(LiteralExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_boolean()

    @property
    def value(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'value'))

    @value.setter
    def value(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('value')
        self.set_property_value(property=property_, value=value)


class LiteralString(LiteralExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_literal_string()

    @property
    def value(self) ->str:
        return cast(str, get_property_value_by_name(self, 'value'))

    @value.setter
    def value(self, value: str):
        property_ = self.get_classifier().require_property_by_name('value')
        self.set_property_value(property=property_, value=value)


class FeatureChainExpression(OperatorExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature_chain_expression()

    @property
    def targetFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'targetFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @targetFeature.setter
    def targetFeature(self, targetFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('targetFeature'
            )
        if self.targetFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(targetFeature,
            targetFeature.name))


class IBindingConnector(Node, ABC):
    pass


class IAssociationStructure(Node, ABC):
    pass


class Dependency(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_dependency()

    @property
    def client(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'client')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_client(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('client'), ReferenceValue(new_element,
            new_element.name))

    @property
    def supplier(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'supplier')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_supplier(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('supplier'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class NamespaceImport(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_namespace_import()

    @property
    def importedNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importedNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importedNamespace.setter
    def importedNamespace(self, importedNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importedNamespace')
        if self.importedNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importedNamespace, importedNamespace.name))

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class MembershipImport(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_membership_import()

    @property
    def importedMembership(self) ->Optional['Membership']:
        res = get_only_reference_value_by_reference_name(self,
            'importedMembership')
        if res:
            return cast(Membership, res.referred)
        else:
            return None

    @importedMembership.setter
    def importedMembership(self, importedMembership: 'Membership'):
        reference = self.get_classifier().get_reference_by_name(
            'importedMembership')
        if self.importedMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importedMembership, importedMembership.name))

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IUsage(Node, ABC):
    pass


class ConnectorAsUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_connector_as_usage()

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class IOccurrenceUsage(Node, ABC):
    pass


class IItemUsage(Node, ABC):
    pass


class IPartUsage(Node, ABC):
    pass


class ConnectionUsage(ConnectorAsUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_connection_usage()

    @property
    def connectionDefinition(self) ->List['IAssociationStructure']:
        res = get_reference_value_by_name(self, 'connectionDefinition')
        return [(cast(IAssociationStructure, r.referred) if r else None) for
            r in res]

    def add_to_connection_definition(self, new_element: 'IAssociationStructure'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectionDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def partDefinition(self) ->List['PartDefinition']:
        res = get_reference_value_by_name(self, 'partDefinition')
        return [(cast(PartDefinition, r.referred) if r else None) for r in res]

    def add_to_part_definition(self, new_element: 'PartDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('partDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class InterfaceUsage(ConnectionUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_interface_usage()

    @property
    def interfaceDefinition(self) ->List['InterfaceDefinition']:
        res = get_reference_value_by_name(self, 'interfaceDefinition')
        return [(cast(InterfaceDefinition, r.referred) if r else None) for
            r in res]

    def add_to_interface_definition(self, new_element: 'InterfaceDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interfaceDefinition'),
            ReferenceValue(new_element, new_element.name))


class VariantMembership(OwningMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_variant_membership()

    @property
    def ownedVariantUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedVariantUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @ownedVariantUsage.setter
    def ownedVariantUsage(self, ownedVariantUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedVariantUsage')
        if self.ownedVariantUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedVariantUsage, ownedVariantUsage.name))


class Definition(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_definition()

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'ownedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_owned_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'ownedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_owned_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'ownedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'ownedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'ownedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_owned_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'ownedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_owned_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'ownedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_owned_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'ownedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'ownedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_owned_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'ownedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_owned_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'ownedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_owned_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'ownedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_owned_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'ownedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_owned_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'ownedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_owned_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'ownedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'ownedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'ownedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'ownedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_owned_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'ownedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_owned_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'ownedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_owned_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnalysisCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'ownedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_owned_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'ownedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_owned_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'ownedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_owned_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'ownedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_owned_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'ownedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_owned_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'ownedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_owned_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'ownedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_owned_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ReferenceUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_reference_usage()

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class AttributeUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_attribute_usage()

    @property
    def attributeDefinition(self) ->List['IDataType']:
        res = get_reference_value_by_name(self, 'attributeDefinition')
        return [(cast(IDataType, r.referred) if r else None) for r in res]

    def add_to_attribute_definition(self, new_element: 'IDataType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('attributeDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class EnumerationUsage(AttributeUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_enumeration_usage()

    @property
    def enumerationDefinition(self) ->Optional['EnumerationDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'enumerationDefinition')
        if res:
            return cast(EnumerationDefinition, res.referred)
        else:
            return None

    @enumerationDefinition.setter
    def enumerationDefinition(self, enumerationDefinition:
        'EnumerationDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'enumerationDefinition')
        if self.enumerationDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            enumerationDefinition, enumerationDefinition.name))


class AttributeDefinition(Definition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_attribute_definition()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class EnumerationDefinition(AttributeDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_enumeration_definition()

    @property
    def enumeratedValue(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'enumeratedValue')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_enumerated_value(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('enumeratedValue'), ReferenceValue(
            new_element, new_element.name))


class OccurrenceDefinition(Definition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_occurrence_definition()

    @property
    def lifeClass(self) ->Optional['LifeClass']:
        res = get_only_reference_value_by_reference_name(self, 'lifeClass')
        if res:
            return cast(LifeClass, res.referred)
        else:
            return None

    @lifeClass.setter
    def lifeClass(self, lifeClass: 'LifeClass'):
        reference = self.get_classifier().get_reference_by_name('lifeClass')
        if self.lifeClass:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(lifeClass,
            lifeClass.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class LifeClass(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_life_class()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ItemDefinition(OccurrenceDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_item_definition()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class PartDefinition(ItemDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_part_definition()


class PortUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_port_usage()

    @property
    def portDefinition(self) ->List['PortDefinition']:
        res = get_reference_value_by_name(self, 'portDefinition')
        return [(cast(PortDefinition, r.referred) if r else None) for r in res]

    def add_to_port_definition(self, new_element: 'PortDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('portDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class PortDefinition(OccurrenceDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_port_definition()

    @property
    def conjugatedPortDefinition(self) ->Optional['ConjugatedPortDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'conjugatedPortDefinition')
        if res:
            return cast(ConjugatedPortDefinition, res.referred)
        else:
            return None

    @conjugatedPortDefinition.setter
    def conjugatedPortDefinition(self, conjugatedPortDefinition:
        'ConjugatedPortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'conjugatedPortDefinition')
        if self.conjugatedPortDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            conjugatedPortDefinition, conjugatedPortDefinition.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ConjugatedPortDefinition(PortDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_conjugated_port_definition()

    @property
    def ownedPortConjugator(self) ->Optional['PortConjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedPortConjugator')
        if res:
            return cast(PortConjugation, res.referred)
        else:
            return None

    @ownedPortConjugator.setter
    def ownedPortConjugator(self, ownedPortConjugator: 'PortConjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedPortConjugator')
        if self.ownedPortConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedPortConjugator, ownedPortConjugator.name))

    @property
    def originalPortDefinition(self) ->Optional['PortDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'originalPortDefinition')
        if res:
            return cast(PortDefinition, res.referred)
        else:
            return None

    @originalPortDefinition.setter
    def originalPortDefinition(self, originalPortDefinition: 'PortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'originalPortDefinition')
        if self.originalPortDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            originalPortDefinition, originalPortDefinition.name))


class PortConjugation(Conjugation):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_port_conjugation()

    @property
    def originalPortDefinition(self) ->Optional['PortDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'originalPortDefinition')
        if res:
            return cast(PortDefinition, res.referred)
        else:
            return None

    @originalPortDefinition.setter
    def originalPortDefinition(self, originalPortDefinition: 'PortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'originalPortDefinition')
        if self.originalPortDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            originalPortDefinition, originalPortDefinition.name))

    @property
    def conjugatedPortDefinition(self) ->Optional['ConjugatedPortDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'conjugatedPortDefinition')
        if res:
            return cast(ConjugatedPortDefinition, res.referred)
        else:
            return None

    @conjugatedPortDefinition.setter
    def conjugatedPortDefinition(self, conjugatedPortDefinition:
        'ConjugatedPortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'conjugatedPortDefinition')
        if self.conjugatedPortDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            conjugatedPortDefinition, conjugatedPortDefinition.name))


class IActionUsage(Node, ABC):
    pass


class FlowConnectionUsage(ConnectorAsUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_flow_connection_usage()

    @property
    def flowConnectionDefinition(self) ->List['IInteraction']:
        res = get_reference_value_by_name(self, 'flowConnectionDefinition')
        return [(cast(IInteraction, r.referred) if r else None) for r in res]

    def add_to_flow_connection_definition(self, new_element: 'IInteraction'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('flowConnectionDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemType(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'itemType')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_item_type(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def targetInputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'targetInputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @targetInputFeature.setter
    def targetInputFeature(self, targetInputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'targetInputFeature')
        if self.targetInputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            targetInputFeature, targetInputFeature.name))

    @property
    def sourceOutputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'sourceOutputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceOutputFeature.setter
    def sourceOutputFeature(self, sourceOutputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'sourceOutputFeature')
        if self.sourceOutputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            sourceOutputFeature, sourceOutputFeature.name))

    @property
    def itemFlowEnd(self) ->List['ItemFlowEnd']:
        res = get_reference_value_by_name(self, 'itemFlowEnd')
        return [(cast(ItemFlowEnd, r.referred) if r else None) for r in res]

    def add_to_item_flow_end(self, new_element: 'ItemFlowEnd'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemFlowEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemFeature(self) ->Optional['ItemFeature']:
        res = get_only_reference_value_by_reference_name(self, 'itemFeature')
        if res:
            return cast(ItemFeature, res.referred)
        else:
            return None

    @itemFeature.setter
    def itemFeature(self, itemFeature: 'ItemFeature'):
        reference = self.get_classifier().get_reference_by_name('itemFeature')
        if self.itemFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(itemFeature,
            itemFeature.name))

    @property
    def interaction(self) ->List['IInteraction']:
        res = get_reference_value_by_name(self, 'interaction')
        return [(cast(IInteraction, r.referred) if r else None) for r in res]

    def add_to_interaction(self, new_element: 'IInteraction'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interaction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class AllocationUsage(ConnectionUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_allocation_usage()

    @property
    def allocationDefinition(self) ->List['AllocationDefinition']:
        res = get_reference_value_by_name(self, 'allocationDefinition')
        return [(cast(AllocationDefinition, r.referred) if r else None) for
            r in res]

    def add_to_allocation_definition(self, new_element: 'AllocationDefinition'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('allocationDefinition'),
            ReferenceValue(new_element, new_element.name))


class ConnectionDefinition(PartDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_connection_definition()

    @property
    def connectionEnd(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'connectionEnd')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_connection_end(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectionEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'relatedType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_related_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'sourceType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @sourceType.setter
    def sourceType(self, sourceType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('sourceType')
        if self.sourceType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceType,
            sourceType.name))

    @property
    def targetType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'targetType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_target_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def associationEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'associationEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_association_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('associationEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class AllocationDefinition(ConnectionDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_allocation_definition()

    @property
    def allocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'allocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('allocation'), ReferenceValue(
            new_element, new_element.name))


class StateUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_state_usage()

    @property
    def stateDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'stateDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_state_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('stateDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def entryAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'entryAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @entryAction.setter
    def entryAction(self, entryAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('entryAction')
        if self.entryAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(entryAction,
            entryAction.name))

    @property
    def doAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'doAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @doAction.setter
    def doAction(self, doAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('doAction')
        if self.doAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(doAction,
            doAction.name))

    @property
    def exitAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'exitAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @exitAction.setter
    def exitAction(self, exitAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('exitAction')
        if self.exitAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(exitAction,
            exitAction.name))

    @property
    def isParallel(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isParallel'))

    @isParallel.setter
    def isParallel(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isParallel'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class TransitionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_transition_usage()

    @property
    def source(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'source')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @source.setter
    def source(self, source: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('source')
        if self.source:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(source, source.name)
            )

    @property
    def target(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'target')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @target.setter
    def target(self, target: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('target')
        if self.target:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(target, target.name)
            )

    @property
    def triggerAction(self) ->List['AcceptActionUsage']:
        res = get_reference_value_by_name(self, 'triggerAction')
        return [(cast(AcceptActionUsage, r.referred) if r else None) for r in
            res]

    def add_to_trigger_action(self, new_element: 'AcceptActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('triggerAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def guardExpression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'guardExpression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_guard_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('guardExpression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def effectAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'effectAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_effect_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('effectAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def succession(self) ->Optional['ISuccession']:
        res = get_only_reference_value_by_reference_name(self, 'succession')
        if res:
            return cast(ISuccession, res.referred)
        else:
            return None

    @succession.setter
    def succession(self, succession: 'ISuccession'):
        reference = self.get_classifier().get_reference_by_name('succession')
        if self.succession:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(succession,
            succession.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class AcceptActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_accept_action_usage()

    @property
    def receiverArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'receiverArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @receiverArgument.setter
    def receiverArgument(self, receiverArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'receiverArgument')
        if self.receiverArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(receiverArgument,
            receiverArgument.name))

    @property
    def payloadParameter(self) ->Optional['ReferenceUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'payloadParameter')
        if res:
            return cast(ReferenceUsage, res.referred)
        else:
            return None

    @payloadParameter.setter
    def payloadParameter(self, payloadParameter: 'ReferenceUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'payloadParameter')
        if self.payloadParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(payloadParameter,
            payloadParameter.name))

    @property
    def payloadArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'payloadArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @payloadArgument.setter
    def payloadArgument(self, payloadArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'payloadArgument')
        if self.payloadArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(payloadArgument,
            payloadArgument.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class CalculationUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_calculation_usage()

    @property
    def calculationDefinition(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self,
            'calculationDefinition')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @calculationDefinition.setter
    def calculationDefinition(self, calculationDefinition: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name(
            'calculationDefinition')
        if self.calculationDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            calculationDefinition, calculationDefinition.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class IConstraintUsage(Node, ABC):
    pass


class TextContainer(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_text_container()

    @property
    def text(self) ->str:
        return cast(str, get_property_value_by_name(self, 'text'))

    @text.setter
    def text(self, value: str):
        property_ = self.get_classifier().require_property_by_name('text')
        self.set_property_value(property=property_, value=value)


class RequirementUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_requirement_usage()

    @property
    def requirementDefinition(self) ->Optional['RequirementDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'requirementDefinition')
        if res:
            return cast(RequirementDefinition, res.referred)
        else:
            return None

    @requirementDefinition.setter
    def requirementDefinition(self, requirementDefinition:
        'RequirementDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'requirementDefinition')
        if self.requirementDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            requirementDefinition, requirementDefinition.name))

    @property
    def reqId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'reqId'))

    @reqId.setter
    def reqId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('reqId')
        self.set_property_value(property=property_, value=value)

    @property
    def requiredConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'requiredConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_required_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('requiredConstraint'), ReferenceValue
            (new_element, new_element.name))

    @property
    def assumedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'assumedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_assumed_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('assumedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def subjectParameter(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'subjectParameter')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @subjectParameter.setter
    def subjectParameter(self, subjectParameter: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'subjectParameter')
        if self.subjectParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subjectParameter,
            subjectParameter.name))

    @property
    def framedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'framedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_framed_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('framedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def actorParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'actorParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_actor_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actorParameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def stakeholderParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'stakeholderParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_stakeholder_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('stakeholderParameter'),
            ReferenceValue(new_element, new_element.name))

    @property
    def constraintDefinition(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self,
            'constraintDefinition')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @constraintDefinition.setter
    def constraintDefinition(self, constraintDefinition: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name(
            'constraintDefinition')
        if self.constraintDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            constraintDefinition, constraintDefinition.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ConstraintDefinition(OccurrenceDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_constraint_definition()

    @property
    def expression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'expression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('expression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class RequirementDefinition(ConstraintDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_requirement_definition()

    @property
    def reqId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'reqId'))

    @reqId.setter
    def reqId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('reqId')
        self.set_property_value(property=property_, value=value)

    @property
    def subjectParameter(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'subjectParameter')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @subjectParameter.setter
    def subjectParameter(self, subjectParameter: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'subjectParameter')
        if self.subjectParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subjectParameter,
            subjectParameter.name))

    @property
    def actorParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'actorParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_actor_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actorParameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def stakeholderParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'stakeholderParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_stakeholder_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('stakeholderParameter'),
            ReferenceValue(new_element, new_element.name))

    @property
    def assumedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'assumedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_assumed_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('assumedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def requiredConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'requiredConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_required_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('requiredConstraint'), ReferenceValue
            (new_element, new_element.name))

    @property
    def framedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'framedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_framed_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('framedConcern'), ReferenceValue(
            new_element, new_element.name))


class ConcernUsage(RequirementUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_concern_usage()

    @property
    def concernDefinition(self) ->Optional['ConcernDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'concernDefinition')
        if res:
            return cast(ConcernDefinition, res.referred)
        else:
            return None

    @concernDefinition.setter
    def concernDefinition(self, concernDefinition: 'ConcernDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'concernDefinition')
        if self.concernDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            concernDefinition, concernDefinition.name))


class ConcernDefinition(RequirementDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_concern_definition()


class CaseUsage(CalculationUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_case_usage()

    @property
    def objectiveRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'objectiveRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @objectiveRequirement.setter
    def objectiveRequirement(self, objectiveRequirement: 'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'objectiveRequirement')
        if self.objectiveRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            objectiveRequirement, objectiveRequirement.name))

    @property
    def caseDefinition(self) ->Optional['CaseDefinition']:
        res = get_only_reference_value_by_reference_name(self, 'caseDefinition'
            )
        if res:
            return cast(CaseDefinition, res.referred)
        else:
            return None

    @caseDefinition.setter
    def caseDefinition(self, caseDefinition: 'CaseDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'caseDefinition')
        if self.caseDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(caseDefinition,
            caseDefinition.name))

    @property
    def subjectParameter(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'subjectParameter')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @subjectParameter.setter
    def subjectParameter(self, subjectParameter: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'subjectParameter')
        if self.subjectParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subjectParameter,
            subjectParameter.name))

    @property
    def actorParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'actorParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_actor_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actorParameter'), ReferenceValue(
            new_element, new_element.name))


class ActionDefinition(OccurrenceDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_action_definition()

    @property
    def action(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'action')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('action'), ReferenceValue(new_element,
            new_element.name))

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class CalculationDefinition(ActionDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_calculation_definition()

    @property
    def calculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'calculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('calculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def expression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'expression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('expression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class CaseDefinition(CalculationDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_case_definition()

    @property
    def objectiveRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'objectiveRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @objectiveRequirement.setter
    def objectiveRequirement(self, objectiveRequirement: 'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'objectiveRequirement')
        if self.objectiveRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            objectiveRequirement, objectiveRequirement.name))

    @property
    def subjectParameter(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'subjectParameter')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @subjectParameter.setter
    def subjectParameter(self, subjectParameter: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'subjectParameter')
        if self.subjectParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(subjectParameter,
            subjectParameter.name))

    @property
    def actorParameter(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'actorParameter')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_actor_parameter(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actorParameter'), ReferenceValue(
            new_element, new_element.name))


class AnalysisCaseUsage(CaseUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_analysis_case_usage()

    @property
    def analysisCaseDefinition(self) ->Optional['AnalysisCaseDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'analysisCaseDefinition')
        if res:
            return cast(AnalysisCaseDefinition, res.referred)
        else:
            return None

    @analysisCaseDefinition.setter
    def analysisCaseDefinition(self, analysisCaseDefinition:
        'AnalysisCaseDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'analysisCaseDefinition')
        if self.analysisCaseDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            analysisCaseDefinition, analysisCaseDefinition.name))

    @property
    def resultExpression(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'resultExpression')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @resultExpression.setter
    def resultExpression(self, resultExpression: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'resultExpression')
        if self.resultExpression:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(resultExpression,
            resultExpression.name))


class AnalysisCaseDefinition(CaseDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_analysis_case_definition()

    @property
    def resultExpression(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'resultExpression')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @resultExpression.setter
    def resultExpression(self, resultExpression: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'resultExpression')
        if self.resultExpression:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(resultExpression,
            resultExpression.name))


class VerificationCaseUsage(CaseUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_verification_case_usage()

    @property
    def verificationCaseDefinition(self) ->Optional[
        'VerificationCaseDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'verificationCaseDefinition')
        if res:
            return cast(VerificationCaseDefinition, res.referred)
        else:
            return None

    @verificationCaseDefinition.setter
    def verificationCaseDefinition(self, verificationCaseDefinition:
        'VerificationCaseDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'verificationCaseDefinition')
        if self.verificationCaseDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            verificationCaseDefinition, verificationCaseDefinition.name))

    @property
    def verifiedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'verifiedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_verified_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('verifiedRequirement'),
            ReferenceValue(new_element, new_element.name))


class VerificationCaseDefinition(CaseDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_verification_case_definition()

    @property
    def verifiedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'verifiedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_verified_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('verifiedRequirement'),
            ReferenceValue(new_element, new_element.name))


class UseCaseUsage(CaseUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_use_case_usage()

    @property
    def useCaseDefinition(self) ->Optional['UseCaseDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'useCaseDefinition')
        if res:
            return cast(UseCaseDefinition, res.referred)
        else:
            return None

    @useCaseDefinition.setter
    def useCaseDefinition(self, useCaseDefinition: 'UseCaseDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'useCaseDefinition')
        if self.useCaseDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            useCaseDefinition, useCaseDefinition.name))

    @property
    def includedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'includedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_included_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('includedUseCase'), ReferenceValue(
            new_element, new_element.name))


class UseCaseDefinition(CaseDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_use_case_definition()

    @property
    def includedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'includedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_included_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('includedUseCase'), ReferenceValue(
            new_element, new_element.name))


class ViewUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_view_usage()

    @property
    def viewDefinition(self) ->Optional['ViewDefinition']:
        res = get_only_reference_value_by_reference_name(self, 'viewDefinition'
            )
        if res:
            return cast(ViewDefinition, res.referred)
        else:
            return None

    @viewDefinition.setter
    def viewDefinition(self, viewDefinition: 'ViewDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'viewDefinition')
        if self.viewDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(viewDefinition,
            viewDefinition.name))

    @property
    def satisfiedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'satisfiedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_satisfied_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('satisfiedViewpoint'), ReferenceValue
            (new_element, new_element.name))

    @property
    def exposedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'exposedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_exposed_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('exposedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def viewRendering(self) ->Optional['RenderingUsage']:
        res = get_only_reference_value_by_reference_name(self, 'viewRendering')
        if res:
            return cast(RenderingUsage, res.referred)
        else:
            return None

    @viewRendering.setter
    def viewRendering(self, viewRendering: 'RenderingUsage'):
        reference = self.get_classifier().get_reference_by_name('viewRendering'
            )
        if self.viewRendering:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(viewRendering,
            viewRendering.name))

    @property
    def viewCondition(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'viewCondition')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_view_condition(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('viewCondition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def partDefinition(self) ->List['PartDefinition']:
        res = get_reference_value_by_name(self, 'partDefinition')
        return [(cast(PartDefinition, r.referred) if r else None) for r in res]

    def add_to_part_definition(self, new_element: 'PartDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('partDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ViewDefinition(PartDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_view_definition()

    @property
    def view(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'view')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('view'), ReferenceValue(new_element,
            new_element.name))

    @property
    def satisfiedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'satisfiedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_satisfied_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('satisfiedViewpoint'), ReferenceValue
            (new_element, new_element.name))

    @property
    def viewRendering(self) ->Optional['RenderingUsage']:
        res = get_only_reference_value_by_reference_name(self, 'viewRendering')
        if res:
            return cast(RenderingUsage, res.referred)
        else:
            return None

    @viewRendering.setter
    def viewRendering(self, viewRendering: 'RenderingUsage'):
        reference = self.get_classifier().get_reference_by_name('viewRendering'
            )
        if self.viewRendering:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(viewRendering,
            viewRendering.name))

    @property
    def viewCondition(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'viewCondition')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_view_condition(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('viewCondition'), ReferenceValue(
            new_element, new_element.name))


class ViewpointUsage(RequirementUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_viewpoint_usage()

    @property
    def viewpointDefinition(self) ->Optional['ViewpointDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'viewpointDefinition')
        if res:
            return cast(ViewpointDefinition, res.referred)
        else:
            return None

    @viewpointDefinition.setter
    def viewpointDefinition(self, viewpointDefinition: 'ViewpointDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'viewpointDefinition')
        if self.viewpointDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            viewpointDefinition, viewpointDefinition.name))

    @property
    def viewpointStakeholder(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'viewpointStakeholder')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_viewpoint_stakeholder(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('viewpointStakeholder'),
            ReferenceValue(new_element, new_element.name))


class ViewpointDefinition(RequirementDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_viewpoint_definition()

    @property
    def viewpointStakeholder(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'viewpointStakeholder')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_viewpoint_stakeholder(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('viewpointStakeholder'),
            ReferenceValue(new_element, new_element.name))


class RenderingUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_rendering_usage()

    @property
    def renderingDefinition(self) ->Optional['RenderingDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'renderingDefinition')
        if res:
            return cast(RenderingDefinition, res.referred)
        else:
            return None

    @renderingDefinition.setter
    def renderingDefinition(self, renderingDefinition: 'RenderingDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'renderingDefinition')
        if self.renderingDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            renderingDefinition, renderingDefinition.name))

    @property
    def partDefinition(self) ->List['PartDefinition']:
        res = get_reference_value_by_name(self, 'partDefinition')
        return [(cast(PartDefinition, r.referred) if r else None) for r in res]

    def add_to_part_definition(self, new_element: 'PartDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('partDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class RenderingDefinition(PartDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_rendering_definition()

    @property
    def rendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'rendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('rendering'), ReferenceValue(
            new_element, new_element.name))


class MetadataUsage(MetadataFeature):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_metadata_usage()

    @property
    def metadataDefinition(self) ->Optional['IMetaclass']:
        res = get_only_reference_value_by_reference_name(self,
            'metadataDefinition')
        if res:
            return cast(IMetaclass, res.referred)
        else:
            return None

    @metadataDefinition.setter
    def metadataDefinition(self, metadataDefinition: 'IMetaclass'):
        reference = self.get_classifier().get_reference_by_name(
            'metadataDefinition')
        if self.metadataDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            metadataDefinition, metadataDefinition.name))

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class InterfaceDefinition(ConnectionDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_interface_definition()

    @property
    def interfaceEnd(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'interfaceEnd')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_interface_end(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interfaceEnd'), ReferenceValue(
            new_element, new_element.name))


class ConjugatedPortTyping(FeatureTyping):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_conjugated_port_typing()

    @property
    def portDefinition(self) ->Optional['PortDefinition']:
        res = get_only_reference_value_by_reference_name(self, 'portDefinition'
            )
        if res:
            return cast(PortDefinition, res.referred)
        else:
            return None

    @portDefinition.setter
    def portDefinition(self, portDefinition: 'PortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'portDefinition')
        if self.portDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(portDefinition,
            portDefinition.name))

    @property
    def conjugatedPortDefinition(self) ->Optional['ConjugatedPortDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'conjugatedPortDefinition')
        if res:
            return cast(ConjugatedPortDefinition, res.referred)
        else:
            return None

    @conjugatedPortDefinition.setter
    def conjugatedPortDefinition(self, conjugatedPortDefinition:
        'ConjugatedPortDefinition'):
        reference = self.get_classifier().get_reference_by_name(
            'conjugatedPortDefinition')
        if self.conjugatedPortDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            conjugatedPortDefinition, conjugatedPortDefinition.name))


class TransitionFeatureMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_transition_feature_membership()

    @property
    def kind(self) ->TransitionFeatureKind:
        return cast(TransitionFeatureKind, get_property_value_by_name(self,
            'kind'))

    @kind.setter
    def kind(self, value: TransitionFeatureKind):
        property_ = self.get_classifier().require_property_by_name('kind')
        self.set_property_value(property=property_, value=value)

    @property
    def transitionFeature(self) ->Optional['IStep']:
        res = get_only_reference_value_by_reference_name(self,
            'transitionFeature')
        if res:
            return cast(IStep, res.referred)
        else:
            return None

    @transitionFeature.setter
    def transitionFeature(self, transitionFeature: 'IStep'):
        reference = self.get_classifier().get_reference_by_name(
            'transitionFeature')
        if self.transitionFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            transitionFeature, transitionFeature.name))


class IEventOccurrenceUsage(Node, ABC):
    pass


class IPerformActionUsage(Node, ABC):
    pass


class ExhibitStateUsage(StateUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_exhibit_state_usage()

    @property
    def exhibitedState(self) ->Optional['StateUsage']:
        res = get_only_reference_value_by_reference_name(self, 'exhibitedState'
            )
        if res:
            return cast(StateUsage, res.referred)
        else:
            return None

    @exhibitedState.setter
    def exhibitedState(self, exhibitedState: 'StateUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'exhibitedState')
        if self.exhibitedState:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(exhibitedState,
            exhibitedState.name))

    @property
    def performedAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'performedAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @performedAction.setter
    def performedAction(self, performedAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'performedAction')
        if self.performedAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(performedAction,
            performedAction.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def eventOccurrence(self) ->Optional['IOccurrenceUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'eventOccurrence')
        if res:
            return cast(IOccurrenceUsage, res.referred)
        else:
            return None

    @eventOccurrence.setter
    def eventOccurrence(self, eventOccurrence: 'IOccurrenceUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'eventOccurrence')
        if self.eventOccurrence:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(eventOccurrence,
            eventOccurrence.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class StateSubactionMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_state_subaction_membership()

    @property
    def kind(self) ->StateSubactionKind:
        return cast(StateSubactionKind, get_property_value_by_name(self,
            'kind'))

    @kind.setter
    def kind(self, value: StateSubactionKind):
        property_ = self.get_classifier().require_property_by_name('kind')
        self.set_property_value(property=property_, value=value)

    @property
    def action(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'action')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @action.setter
    def action(self, action: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('action')
        if self.action:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(action, action.name)
            )


class StateDefinition(ActionDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_state_definition()

    @property
    def state(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'state')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('state'), ReferenceValue(new_element,
            new_element.name))

    @property
    def entryAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'entryAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @entryAction.setter
    def entryAction(self, entryAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('entryAction')
        if self.entryAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(entryAction,
            entryAction.name))

    @property
    def doAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'doAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @doAction.setter
    def doAction(self, doAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('doAction')
        if self.doAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(doAction,
            doAction.name))

    @property
    def exitAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'exitAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @exitAction.setter
    def exitAction(self, exitAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('exitAction')
        if self.exitAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(exitAction,
            exitAction.name))

    @property
    def isParallel(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isParallel'))

    @isParallel.setter
    def isParallel(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isParallel'
            )
        self.set_property_value(property=property_, value=value)


class SuccessionFlowConnectionUsage(FlowConnectionUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_succession_flow_connection_usage()

    @property
    def itemType(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'itemType')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_item_type(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def targetInputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'targetInputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @targetInputFeature.setter
    def targetInputFeature(self, targetInputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'targetInputFeature')
        if self.targetInputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            targetInputFeature, targetInputFeature.name))

    @property
    def sourceOutputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'sourceOutputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceOutputFeature.setter
    def sourceOutputFeature(self, sourceOutputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'sourceOutputFeature')
        if self.sourceOutputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            sourceOutputFeature, sourceOutputFeature.name))

    @property
    def itemFlowEnd(self) ->List['ItemFlowEnd']:
        res = get_reference_value_by_name(self, 'itemFlowEnd')
        return [(cast(ItemFlowEnd, r.referred) if r else None) for r in res]

    def add_to_item_flow_end(self, new_element: 'ItemFlowEnd'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemFlowEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemFeature(self) ->Optional['ItemFeature']:
        res = get_only_reference_value_by_reference_name(self, 'itemFeature')
        if res:
            return cast(ItemFeature, res.referred)
        else:
            return None

    @itemFeature.setter
    def itemFeature(self, itemFeature: 'ItemFeature'):
        reference = self.get_classifier().get_reference_by_name('itemFeature')
        if self.itemFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(itemFeature,
            itemFeature.name))

    @property
    def interaction(self) ->List['IInteraction']:
        res = get_reference_value_by_name(self, 'interaction')
        return [(cast(IInteraction, r.referred) if r else None) for r in res]

    def add_to_interaction(self, new_element: 'IInteraction'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interaction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def transitionStep(self) ->Optional['IStep']:
        res = get_only_reference_value_by_reference_name(self, 'transitionStep'
            )
        if res:
            return cast(IStep, res.referred)
        else:
            return None

    @transitionStep.setter
    def transitionStep(self, transitionStep: 'IStep'):
        reference = self.get_classifier().get_reference_by_name(
            'transitionStep')
        if self.transitionStep:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(transitionStep,
            transitionStep.name))

    @property
    def triggerStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'triggerStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_trigger_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('triggerStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def effectStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'effectStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_effect_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('effectStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def guardExpression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'guardExpression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_guard_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('guardExpression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class FlowConnectionDefinition(ActionDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_flow_connection_definition()

    @property
    def flowConnectionEnd(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'flowConnectionEnd')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_flow_connection_end(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('flowConnectionEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'relatedType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_related_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'sourceType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @sourceType.setter
    def sourceType(self, sourceType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('sourceType')
        if self.sourceType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceType,
            sourceType.name))

    @property
    def targetType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'targetType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_target_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def associationEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'associationEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_association_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('associationEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class RequirementConstraintMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_requirement_constraint_membership()

    @property
    def kind(self) ->RequirementConstraintKind:
        return cast(RequirementConstraintKind, get_property_value_by_name(
            self, 'kind'))

    @kind.setter
    def kind(self, value: RequirementConstraintKind):
        property_ = self.get_classifier().require_property_by_name('kind')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConstraint(self) ->Optional['IConstraintUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConstraint')
        if res:
            return cast(IConstraintUsage, res.referred)
        else:
            return None

    @ownedConstraint.setter
    def ownedConstraint(self, ownedConstraint: 'IConstraintUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConstraint')
        if self.ownedConstraint:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConstraint,
            ownedConstraint.name))

    @property
    def referencedConstraint(self) ->Optional['IConstraintUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'referencedConstraint')
        if res:
            return cast(IConstraintUsage, res.referred)
        else:
            return None

    @referencedConstraint.setter
    def referencedConstraint(self, referencedConstraint: 'IConstraintUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'referencedConstraint')
        if self.referencedConstraint:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencedConstraint, referencedConstraint.name))


class RequirementVerificationMembership(RequirementConstraintMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_requirement_verification_membership()

    @property
    def ownedRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @ownedRequirement.setter
    def ownedRequirement(self, ownedRequirement: 'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedRequirement')
        if self.ownedRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedRequirement,
            ownedRequirement.name))

    @property
    def verifiedRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'verifiedRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @verifiedRequirement.setter
    def verifiedRequirement(self, verifiedRequirement: 'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'verifiedRequirement')
        if self.verifiedRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            verifiedRequirement, verifiedRequirement.name))


class IncludeUseCaseUsage(UseCaseUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_include_use_case_usage()

    @property
    def useCaseIncluded(self) ->Optional['UseCaseUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'useCaseIncluded')
        if res:
            return cast(UseCaseUsage, res.referred)
        else:
            return None

    @useCaseIncluded.setter
    def useCaseIncluded(self, useCaseIncluded: 'UseCaseUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'useCaseIncluded')
        if self.useCaseIncluded:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(useCaseIncluded,
            useCaseIncluded.name))

    @property
    def performedAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'performedAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @performedAction.setter
    def performedAction(self, performedAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'performedAction')
        if self.performedAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(performedAction,
            performedAction.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def eventOccurrence(self) ->Optional['IOccurrenceUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'eventOccurrence')
        if res:
            return cast(IOccurrenceUsage, res.referred)
        else:
            return None

    @eventOccurrence.setter
    def eventOccurrence(self, eventOccurrence: 'IOccurrenceUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'eventOccurrence')
        if self.eventOccurrence:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(eventOccurrence,
            eventOccurrence.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ObjectiveMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_objective_membership()

    @property
    def ownedObjectiveRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedObjectiveRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @ownedObjectiveRequirement.setter
    def ownedObjectiveRequirement(self, ownedObjectiveRequirement:
        'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedObjectiveRequirement')
        if self.ownedObjectiveRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedObjectiveRequirement, ownedObjectiveRequirement.name))


class IAssertConstraintUsage(Node, ABC):
    pass


class SatisfyRequirementUsage(RequirementUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_satisfy_requirement_usage()

    @property
    def satisfiedRequirement(self) ->Optional['RequirementUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'satisfiedRequirement')
        if res:
            return cast(RequirementUsage, res.referred)
        else:
            return None

    @satisfiedRequirement.setter
    def satisfiedRequirement(self, satisfiedRequirement: 'RequirementUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'satisfiedRequirement')
        if self.satisfiedRequirement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            satisfiedRequirement, satisfiedRequirement.name))

    @property
    def satisfyingFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'satisfyingFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @satisfyingFeature.setter
    def satisfyingFeature(self, satisfyingFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'satisfyingFeature')
        if self.satisfyingFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            satisfyingFeature, satisfyingFeature.name))

    @property
    def assertedConstraint(self) ->Optional['IConstraintUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'assertedConstraint')
        if res:
            return cast(IConstraintUsage, res.referred)
        else:
            return None

    @assertedConstraint.setter
    def assertedConstraint(self, assertedConstraint: 'IConstraintUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'assertedConstraint')
        if self.assertedConstraint:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            assertedConstraint, assertedConstraint.name))

    @property
    def constraintDefinition(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self,
            'constraintDefinition')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @constraintDefinition.setter
    def constraintDefinition(self, constraintDefinition: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name(
            'constraintDefinition')
        if self.constraintDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            constraintDefinition, constraintDefinition.name))

    @property
    def isNegated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNegated'))

    @isNegated.setter
    def isNegated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isNegated')
        self.set_property_value(property=property_, value=value)

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class SubjectMembership(ParameterMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_subject_membership()

    @property
    def ownedSubjectParameter(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedSubjectParameter')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @ownedSubjectParameter.setter
    def ownedSubjectParameter(self, ownedSubjectParameter: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedSubjectParameter')
        if self.ownedSubjectParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedSubjectParameter, ownedSubjectParameter.name))


class StakeholderMembership(ParameterMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_stakeholder_membership()

    @property
    def ownedStakeholderParameter(self) ->Optional['IPartUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedStakeholderParameter')
        if res:
            return cast(IPartUsage, res.referred)
        else:
            return None

    @ownedStakeholderParameter.setter
    def ownedStakeholderParameter(self, ownedStakeholderParameter: 'IPartUsage'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'ownedStakeholderParameter')
        if self.ownedStakeholderParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedStakeholderParameter, ownedStakeholderParameter.name))


class FramedConcernMembership(RequirementConstraintMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_framed_concern_membership()

    @property
    def ownedConcern(self) ->Optional['ConcernUsage']:
        res = get_only_reference_value_by_reference_name(self, 'ownedConcern')
        if res:
            return cast(ConcernUsage, res.referred)
        else:
            return None

    @ownedConcern.setter
    def ownedConcern(self, ownedConcern: 'ConcernUsage'):
        reference = self.get_classifier().get_reference_by_name('ownedConcern')
        if self.ownedConcern:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConcern,
            ownedConcern.name))

    @property
    def referencedConcern(self) ->Optional['ConcernUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'referencedConcern')
        if res:
            return cast(ConcernUsage, res.referred)
        else:
            return None

    @referencedConcern.setter
    def referencedConcern(self, referencedConcern: 'ConcernUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'referencedConcern')
        if self.referencedConcern:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencedConcern, referencedConcern.name))


class ActorMembership(ParameterMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_actor_membership()

    @property
    def ownedActorParameter(self) ->Optional['IPartUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedActorParameter')
        if res:
            return cast(IPartUsage, res.referred)
        else:
            return None

    @ownedActorParameter.setter
    def ownedActorParameter(self, ownedActorParameter: 'IPartUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedActorParameter')
        if self.ownedActorParameter:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedActorParameter, ownedActorParameter.name))


class ViewRenderingMembership(FeatureMembership):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_view_rendering_membership()

    @property
    def ownedRendering(self) ->Optional['RenderingUsage']:
        res = get_only_reference_value_by_reference_name(self, 'ownedRendering'
            )
        if res:
            return cast(RenderingUsage, res.referred)
        else:
            return None

    @ownedRendering.setter
    def ownedRendering(self, ownedRendering: 'RenderingUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedRendering')
        if self.ownedRendering:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedRendering,
            ownedRendering.name))

    @property
    def referencedRendering(self) ->Optional['RenderingUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'referencedRendering')
        if res:
            return cast(RenderingUsage, res.referred)
        else:
            return None

    @referencedRendering.setter
    def referencedRendering(self, referencedRendering: 'RenderingUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'referencedRendering')
        if self.referencedRendering:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            referencedRendering, referencedRendering.name))


class IExpose(Node, ABC):
    pass


class NamespaceExpose(NamespaceImport):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_namespace_expose()

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class MembershipExpose(MembershipImport):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_membership_expose()

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class BindingConnectorAsUsage(ConnectorAsUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_binding_connector_as_usage()

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class SuccessionAsUsage(ConnectorAsUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_succession_as_usage()

    @property
    def transitionStep(self) ->Optional['IStep']:
        res = get_only_reference_value_by_reference_name(self, 'transitionStep'
            )
        if res:
            return cast(IStep, res.referred)
        else:
            return None

    @transitionStep.setter
    def transitionStep(self, transitionStep: 'IStep'):
        reference = self.get_classifier().get_reference_by_name(
            'transitionStep')
        if self.transitionStep:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(transitionStep,
            transitionStep.name))

    @property
    def triggerStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'triggerStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_trigger_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('triggerStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def effectStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'effectStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_effect_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('effectStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def guardExpression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'guardExpression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_guard_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('guardExpression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class ControlNode(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_control_node()

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ForkNode(ControlNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_fork_node()


class JoinNode(ControlNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_join_node()


class SendActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_send_action_usage()

    @property
    def receiverArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'receiverArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @receiverArgument.setter
    def receiverArgument(self, receiverArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'receiverArgument')
        if self.receiverArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(receiverArgument,
            receiverArgument.name))

    @property
    def payloadArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'payloadArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @payloadArgument.setter
    def payloadArgument(self, payloadArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'payloadArgument')
        if self.payloadArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(payloadArgument,
            payloadArgument.name))

    @property
    def senderArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'senderArgument'
            )
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @senderArgument.setter
    def senderArgument(self, senderArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'senderArgument')
        if self.senderArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(senderArgument,
            senderArgument.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class DecisionNode(ControlNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_decision_node()


class MergeNode(ControlNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_merge_node()


class LoopActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_loop_action_usage()

    @property
    def bodyAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'bodyAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @bodyAction.setter
    def bodyAction(self, bodyAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('bodyAction')
        if self.bodyAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(bodyAction,
            bodyAction.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class TriggerInvocationExpression(InvocationExpression):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_trigger_invocation_expression()

    @property
    def kind(self) ->TriggerKind:
        return cast(TriggerKind, get_property_value_by_name(self, 'kind'))

    @kind.setter
    def kind(self, value: TriggerKind):
        property_ = self.get_classifier().require_property_by_name('kind')
        self.set_property_value(property=property_, value=value)


class AssignmentActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_assignment_action_usage()

    @property
    def targetArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'targetArgument'
            )
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @targetArgument.setter
    def targetArgument(self, targetArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'targetArgument')
        if self.targetArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(targetArgument,
            targetArgument.name))

    @property
    def valueExpression(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'valueExpression')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @valueExpression.setter
    def valueExpression(self, valueExpression: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'valueExpression')
        if self.valueExpression:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(valueExpression,
            valueExpression.name))

    @property
    def referent(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'referent')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @referent.setter
    def referent(self, referent: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('referent')
        if self.referent:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(referent,
            referent.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ForLoopActionUsage(LoopActionUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_for_loop_action_usage()

    @property
    def seqArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'seqArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @seqArgument.setter
    def seqArgument(self, seqArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('seqArgument')
        if self.seqArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(seqArgument,
            seqArgument.name))

    @property
    def loopVariable(self) ->Optional['ReferenceUsage']:
        res = get_only_reference_value_by_reference_name(self, 'loopVariable')
        if res:
            return cast(ReferenceUsage, res.referred)
        else:
            return None

    @loopVariable.setter
    def loopVariable(self, loopVariable: 'ReferenceUsage'):
        reference = self.get_classifier().get_reference_by_name('loopVariable')
        if self.loopVariable:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(loopVariable,
            loopVariable.name))


class IfActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_if_action_usage()

    @property
    def elseAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'elseAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @elseAction.setter
    def elseAction(self, elseAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('elseAction')
        if self.elseAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(elseAction,
            elseAction.name))

    @property
    def thenAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self, 'thenAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @thenAction.setter
    def thenAction(self, thenAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name('thenAction')
        if self.thenAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(thenAction,
            thenAction.name))

    @property
    def ifArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'ifArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @ifArgument.setter
    def ifArgument(self, ifArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('ifArgument')
        if self.ifArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ifArgument,
            ifArgument.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class WhileLoopActionUsage(LoopActionUsage):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_while_loop_action_usage()

    @property
    def whileArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'whileArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @whileArgument.setter
    def whileArgument(self, whileArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('whileArgument'
            )
        if self.whileArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(whileArgument,
            whileArgument.name))

    @property
    def untilArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self, 'untilArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @untilArgument.setter
    def untilArgument(self, untilArgument: 'IExpression'):
        reference = self.get_classifier().get_reference_by_name('untilArgument'
            )
        if self.untilArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(untilArgument,
            untilArgument.name))


class TerminateActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_terminate_action_usage()

    @property
    def terminatedOccurrenceArgument(self) ->Optional['IExpression']:
        res = get_only_reference_value_by_reference_name(self,
            'terminatedOccurrenceArgument')
        if res:
            return cast(IExpression, res.referred)
        else:
            return None

    @terminatedOccurrenceArgument.setter
    def terminatedOccurrenceArgument(self, terminatedOccurrenceArgument:
        'IExpression'):
        reference = self.get_classifier().get_reference_by_name(
            'terminatedOccurrenceArgument')
        if self.terminatedOccurrenceArgument:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            terminatedOccurrenceArgument, terminatedOccurrenceArgument.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class MetadataDefinition(ItemDefinition):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_metadata_definition()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Featuring(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_featuring()

    @property
    def type(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'type')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @type.setter
    def type(self, type: 'IType'):
        reference = self.get_classifier().get_reference_by_name('type')
        if self.type:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(type, type.name))

    @property
    def feature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'feature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @feature.setter
    def feature(self, feature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('feature')
        if self.feature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(feature, feature
            .name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Relationship(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_relationship()

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Element(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_element()

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class AnnotatingElement(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_annotating_element()

    @property
    def annotatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'annotatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_annotated_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotatingRelationship(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotatingRelationship')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotating_relationship(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotatingRelationship'),
            ReferenceValue(new_element, new_element.name))

    @property
    def annotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'annotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('annotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningAnnotatingRelationship(self) ->Optional['Annotation']:
        res = get_only_reference_value_by_reference_name(self,
            'owningAnnotatingRelationship')
        if res:
            return cast(Annotation, res.referred)
        else:
            return None

    @owningAnnotatingRelationship.setter
    def owningAnnotatingRelationship(self, owningAnnotatingRelationship:
        'Annotation'):
        reference = self.get_classifier().get_reference_by_name(
            'owningAnnotatingRelationship')
        if self.owningAnnotatingRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningAnnotatingRelationship, owningAnnotatingRelationship.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Step(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_step()

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Feature(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_feature()

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Type(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_type()

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Namespace(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_namespace()

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Behavior(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_behavior()

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Class(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_class()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Classifier(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_classifier()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Succession(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_succession()

    @property
    def transitionStep(self) ->Optional['IStep']:
        res = get_only_reference_value_by_reference_name(self, 'transitionStep'
            )
        if res:
            return cast(IStep, res.referred)
        else:
            return None

    @transitionStep.setter
    def transitionStep(self, transitionStep: 'IStep'):
        reference = self.get_classifier().get_reference_by_name(
            'transitionStep')
        if self.transitionStep:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(transitionStep,
            transitionStep.name))

    @property
    def triggerStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'triggerStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_trigger_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('triggerStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def effectStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'effectStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_effect_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('effectStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def guardExpression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'guardExpression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_guard_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('guardExpression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Connector(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_connector()

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Structure(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_structure()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class PartUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_part_usage()

    @property
    def partDefinition(self) ->List['PartDefinition']:
        res = get_reference_value_by_name(self, 'partDefinition')
        return [(cast(PartDefinition, r.referred) if r else None) for r in res]

    def add_to_part_definition(self, new_element: 'PartDefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('partDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ItemUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_item_usage()

    @property
    def itemDefinition(self) ->List['IStructure']:
        res = get_reference_value_by_name(self, 'itemDefinition')
        return [(cast(IStructure, r.referred) if r else None) for r in res]

    def add_to_item_definition(self, new_element: 'IStructure'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class OccurrenceUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_occurrence_usage()

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Usage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_usage()

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class DataType(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_data_type()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_action_usage()

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ItemFlow(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_item_flow()

    @property
    def itemType(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'itemType')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_item_type(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def targetInputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'targetInputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @targetInputFeature.setter
    def targetInputFeature(self, targetInputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'targetInputFeature')
        if self.targetInputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            targetInputFeature, targetInputFeature.name))

    @property
    def sourceOutputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'sourceOutputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceOutputFeature.setter
    def sourceOutputFeature(self, sourceOutputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'sourceOutputFeature')
        if self.sourceOutputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            sourceOutputFeature, sourceOutputFeature.name))

    @property
    def itemFlowEnd(self) ->List['ItemFlowEnd']:
        res = get_reference_value_by_name(self, 'itemFlowEnd')
        return [(cast(ItemFlowEnd, r.referred) if r else None) for r in res]

    def add_to_item_flow_end(self, new_element: 'ItemFlowEnd'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemFlowEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemFeature(self) ->Optional['ItemFeature']:
        res = get_only_reference_value_by_reference_name(self, 'itemFeature')
        if res:
            return cast(ItemFeature, res.referred)
        else:
            return None

    @itemFeature.setter
    def itemFeature(self, itemFeature: 'ItemFeature'):
        reference = self.get_classifier().get_reference_by_name('itemFeature')
        if self.itemFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(itemFeature,
            itemFeature.name))

    @property
    def interaction(self) ->List['IInteraction']:
        res = get_reference_value_by_name(self, 'interaction')
        return [(cast(IInteraction, r.referred) if r else None) for r in res]

    def add_to_interaction(self, new_element: 'IInteraction'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interaction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class AssociationStructure(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_association_structure()

    @property
    def relatedType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'relatedType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_related_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'sourceType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @sourceType.setter
    def sourceType(self, sourceType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('sourceType')
        if self.sourceType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceType,
            sourceType.name))

    @property
    def targetType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'targetType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_target_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def associationEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'associationEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_association_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('associationEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Association(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_association()

    @property
    def relatedType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'relatedType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_related_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'sourceType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @sourceType.setter
    def sourceType(self, sourceType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('sourceType')
        if self.sourceType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceType,
            sourceType.name))

    @property
    def targetType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'targetType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_target_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def associationEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'associationEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_association_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('associationEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Predicate(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_predicate()

    @property
    def expression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'expression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('expression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Function(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_function()

    @property
    def expression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'expression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('expression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class PerformActionUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_perform_action_usage()

    @property
    def performedAction(self) ->Optional['IActionUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'performedAction')
        if res:
            return cast(IActionUsage, res.referred)
        else:
            return None

    @performedAction.setter
    def performedAction(self, performedAction: 'IActionUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'performedAction')
        if self.performedAction:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(performedAction,
            performedAction.name))

    @property
    def actionDefinition(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'actionDefinition')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_action_definition(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('actionDefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def eventOccurrence(self) ->Optional['IOccurrenceUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'eventOccurrence')
        if res:
            return cast(IOccurrenceUsage, res.referred)
        else:
            return None

    @eventOccurrence.setter
    def eventOccurrence(self, eventOccurrence: 'IOccurrenceUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'eventOccurrence')
        if self.eventOccurrence:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(eventOccurrence,
            eventOccurrence.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class EventOccurrenceUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_event_occurrence_usage()

    @property
    def eventOccurrence(self) ->Optional['IOccurrenceUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'eventOccurrence')
        if res:
            return cast(IOccurrenceUsage, res.referred)
        else:
            return None

    @eventOccurrence.setter
    def eventOccurrence(self, eventOccurrence: 'IOccurrenceUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'eventOccurrence')
        if self.eventOccurrence:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(eventOccurrence,
            eventOccurrence.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class SuccessionItemFlow(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_succession_item_flow()

    @property
    def itemType(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'itemType')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_item_type(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def targetInputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'targetInputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @targetInputFeature.setter
    def targetInputFeature(self, targetInputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'targetInputFeature')
        if self.targetInputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            targetInputFeature, targetInputFeature.name))

    @property
    def sourceOutputFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self,
            'sourceOutputFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceOutputFeature.setter
    def sourceOutputFeature(self, sourceOutputFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name(
            'sourceOutputFeature')
        if self.sourceOutputFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            sourceOutputFeature, sourceOutputFeature.name))

    @property
    def itemFlowEnd(self) ->List['ItemFlowEnd']:
        res = get_reference_value_by_name(self, 'itemFlowEnd')
        return [(cast(ItemFlowEnd, r.referred) if r else None) for r in res]

    def add_to_item_flow_end(self, new_element: 'ItemFlowEnd'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('itemFlowEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def itemFeature(self) ->Optional['ItemFeature']:
        res = get_only_reference_value_by_reference_name(self, 'itemFeature')
        if res:
            return cast(ItemFeature, res.referred)
        else:
            return None

    @itemFeature.setter
    def itemFeature(self, itemFeature: 'ItemFeature'):
        reference = self.get_classifier().get_reference_by_name('itemFeature')
        if self.itemFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(itemFeature,
            itemFeature.name))

    @property
    def interaction(self) ->List['IInteraction']:
        res = get_reference_value_by_name(self, 'interaction')
        return [(cast(IInteraction, r.referred) if r else None) for r in res]

    def add_to_interaction(self, new_element: 'IInteraction'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('interaction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def transitionStep(self) ->Optional['IStep']:
        res = get_only_reference_value_by_reference_name(self, 'transitionStep'
            )
        if res:
            return cast(IStep, res.referred)
        else:
            return None

    @transitionStep.setter
    def transitionStep(self, transitionStep: 'IStep'):
        reference = self.get_classifier().get_reference_by_name(
            'transitionStep')
        if self.transitionStep:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(transitionStep,
            transitionStep.name))

    @property
    def triggerStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'triggerStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_trigger_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('triggerStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def effectStep(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'effectStep')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_effect_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('effectStep'), ReferenceValue(
            new_element, new_element.name))

    @property
    def guardExpression(self) ->List['IExpression']:
        res = get_reference_value_by_name(self, 'guardExpression')
        return [(cast(IExpression, r.referred) if r else None) for r in res]

    def add_to_guard_expression(self, new_element: 'IExpression'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('guardExpression'), ReferenceValue(
            new_element, new_element.name))

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Interaction(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_interaction()

    @property
    def relatedType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'relatedType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_related_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'sourceType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @sourceType.setter
    def sourceType(self, sourceType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('sourceType')
        if self.sourceType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceType,
            sourceType.name))

    @property
    def targetType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'targetType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_target_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def associationEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'associationEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_association_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('associationEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def step(self) ->List['IStep']:
        res = get_reference_value_by_name(self, 'step')
        return [(cast(IStep, r.referred) if r else None) for r in res]

    def add_to_step(self, new_element: 'IStep'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('step'), ReferenceValue(new_element,
            new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class AssertConstraintUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_assert_constraint_usage()

    @property
    def assertedConstraint(self) ->Optional['IConstraintUsage']:
        res = get_only_reference_value_by_reference_name(self,
            'assertedConstraint')
        if res:
            return cast(IConstraintUsage, res.referred)
        else:
            return None

    @assertedConstraint.setter
    def assertedConstraint(self, assertedConstraint: 'IConstraintUsage'):
        reference = self.get_classifier().get_reference_by_name(
            'assertedConstraint')
        if self.assertedConstraint:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            assertedConstraint, assertedConstraint.name))

    @property
    def constraintDefinition(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self,
            'constraintDefinition')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @constraintDefinition.setter
    def constraintDefinition(self, constraintDefinition: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name(
            'constraintDefinition')
        if self.constraintDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            constraintDefinition, constraintDefinition.name))

    @property
    def isNegated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNegated'))

    @isNegated.setter
    def isNegated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isNegated')
        self.set_property_value(property=property_, value=value)

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class ConstraintUsage(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_constraint_usage()

    @property
    def constraintDefinition(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self,
            'constraintDefinition')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @constraintDefinition.setter
    def constraintDefinition(self, constraintDefinition: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name(
            'constraintDefinition')
        if self.constraintDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            constraintDefinition, constraintDefinition.name))

    @property
    def occurrenceDefinition(self) ->List['IClass']:
        res = get_reference_value_by_name(self, 'occurrenceDefinition')
        return [(cast(IClass, r.referred) if r else None) for r in res]

    def add_to_occurrence_definition(self, new_element: 'IClass'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('occurrenceDefinition'),
            ReferenceValue(new_element, new_element.name))

    @property
    def individualDefinition(self) ->Optional['OccurrenceDefinition']:
        res = get_only_reference_value_by_reference_name(self,
            'individualDefinition')
        if res:
            return cast(OccurrenceDefinition, res.referred)
        else:
            return None

    @individualDefinition.setter
    def individualDefinition(self, individualDefinition: 'OccurrenceDefinition'
        ):
        reference = self.get_classifier().get_reference_by_name(
            'individualDefinition')
        if self.individualDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            individualDefinition, individualDefinition.name))

    @property
    def isIndividual(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isIndividual'))

    @isIndividual.setter
    def isIndividual(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isIndividual')
        self.set_property_value(property=property_, value=value)

    @property
    def portionKind(self) ->PortionKind:
        return cast(PortionKind, get_property_value_by_name(self,
            'portionKind'))

    @portionKind.setter
    def portionKind(self, value: PortionKind):
        property_ = self.get_classifier().require_property_by_name(
            'portionKind')
        self.set_property_value(property=property_, value=value)

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def isReference(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReference'))

    @isReference.setter
    def isReference(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isReference')
        self.set_property_value(property=property_, value=value)

    @property
    def isVariation(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isVariation'))

    @isVariation.setter
    def isVariation(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isVariation')
        self.set_property_value(property=property_, value=value)

    @property
    def variant(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'variant')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_variant(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variant'), ReferenceValue(
            new_element, new_element.name))

    @property
    def variantMembership(self) ->List['VariantMembership']:
        res = get_reference_value_by_name(self, 'variantMembership')
        return [(cast(VariantMembership, r.referred) if r else None) for r in
            res]

    def add_to_variant_membership(self, new_element: 'VariantMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('variantMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningDefinition(self) ->Optional['Definition']:
        res = get_only_reference_value_by_reference_name(self,
            'owningDefinition')
        if res:
            return cast(Definition, res.referred)
        else:
            return None

    @owningDefinition.setter
    def owningDefinition(self, owningDefinition: 'Definition'):
        reference = self.get_classifier().get_reference_by_name(
            'owningDefinition')
        if self.owningDefinition:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningDefinition,
            owningDefinition.name))

    @property
    def owningUsage(self) ->Optional['IUsage']:
        res = get_only_reference_value_by_reference_name(self, 'owningUsage')
        if res:
            return cast(IUsage, res.referred)
        else:
            return None

    @owningUsage.setter
    def owningUsage(self, owningUsage: 'IUsage'):
        reference = self.get_classifier().get_reference_by_name('owningUsage')
        if self.owningUsage:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningUsage,
            owningUsage.name))

    @property
    def nestedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'nestedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_nested_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def definition(self) ->List['IClassifier']:
        res = get_reference_value_by_name(self, 'definition')
        return [(cast(IClassifier, r.referred) if r else None) for r in res]

    def add_to_definition(self, new_element: 'IClassifier'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('definition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def usage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'usage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('usage'), ReferenceValue(new_element,
            new_element.name))

    @property
    def directedUsage(self) ->List['IUsage']:
        res = get_reference_value_by_name(self, 'directedUsage')
        return [(cast(IUsage, r.referred) if r else None) for r in res]

    def add_to_directed_usage(self, new_element: 'IUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedUsage'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedReference(self) ->List['ReferenceUsage']:
        res = get_reference_value_by_name(self, 'nestedReference')
        return [(cast(ReferenceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_reference(self, new_element: 'ReferenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedReference'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAttribute(self) ->List['AttributeUsage']:
        res = get_reference_value_by_name(self, 'nestedAttribute')
        return [(cast(AttributeUsage, r.referred) if r else None) for r in res]

    def add_to_nested_attribute(self, new_element: 'AttributeUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAttribute'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedEnumeration(self) ->List['EnumerationUsage']:
        res = get_reference_value_by_name(self, 'nestedEnumeration')
        return [(cast(EnumerationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_enumeration(self, new_element: 'EnumerationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedEnumeration'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedOccurrence(self) ->List['IOccurrenceUsage']:
        res = get_reference_value_by_name(self, 'nestedOccurrence')
        return [(cast(IOccurrenceUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_occurrence(self, new_element: 'IOccurrenceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedOccurrence'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedItem(self) ->List['IItemUsage']:
        res = get_reference_value_by_name(self, 'nestedItem')
        return [(cast(IItemUsage, r.referred) if r else None) for r in res]

    def add_to_nested_item(self, new_element: 'IItemUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedItem'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPart(self) ->List['IPartUsage']:
        res = get_reference_value_by_name(self, 'nestedPart')
        return [(cast(IPartUsage, r.referred) if r else None) for r in res]

    def add_to_nested_part(self, new_element: 'IPartUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPart'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedPort(self) ->List['PortUsage']:
        res = get_reference_value_by_name(self, 'nestedPort')
        return [(cast(PortUsage, r.referred) if r else None) for r in res]

    def add_to_nested_port(self, new_element: 'PortUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedPort'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConnection(self) ->List['ConnectorAsUsage']:
        res = get_reference_value_by_name(self, 'nestedConnection')
        return [(cast(ConnectorAsUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_connection(self, new_element: 'ConnectorAsUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConnection'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedFlow(self) ->List['FlowConnectionUsage']:
        res = get_reference_value_by_name(self, 'nestedFlow')
        return [(cast(FlowConnectionUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_flow(self, new_element: 'FlowConnectionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedFlow'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedInterface(self) ->List['InterfaceUsage']:
        res = get_reference_value_by_name(self, 'nestedInterface')
        return [(cast(InterfaceUsage, r.referred) if r else None) for r in res]

    def add_to_nested_interface(self, new_element: 'InterfaceUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedInterface'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAllocation(self) ->List['AllocationUsage']:
        res = get_reference_value_by_name(self, 'nestedAllocation')
        return [(cast(AllocationUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_allocation(self, new_element: 'AllocationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAllocation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAction(self) ->List['IActionUsage']:
        res = get_reference_value_by_name(self, 'nestedAction')
        return [(cast(IActionUsage, r.referred) if r else None) for r in res]

    def add_to_nested_action(self, new_element: 'IActionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAction'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedState(self) ->List['StateUsage']:
        res = get_reference_value_by_name(self, 'nestedState')
        return [(cast(StateUsage, r.referred) if r else None) for r in res]

    def add_to_nested_state(self, new_element: 'StateUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedState'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedTransition(self) ->List['TransitionUsage']:
        res = get_reference_value_by_name(self, 'nestedTransition')
        return [(cast(TransitionUsage, r.referred) if r else None) for r in res
            ]

    def add_to_nested_transition(self, new_element: 'TransitionUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedTransition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCalculation(self) ->List['CalculationUsage']:
        res = get_reference_value_by_name(self, 'nestedCalculation')
        return [(cast(CalculationUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_calculation(self, new_element: 'CalculationUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCalculation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConstraint(self) ->List['IConstraintUsage']:
        res = get_reference_value_by_name(self, 'nestedConstraint')
        return [(cast(IConstraintUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_constraint(self, new_element: 'IConstraintUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConstraint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRequirement(self) ->List['RequirementUsage']:
        res = get_reference_value_by_name(self, 'nestedRequirement')
        return [(cast(RequirementUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_requirement(self, new_element: 'RequirementUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRequirement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedConcern(self) ->List['ConcernUsage']:
        res = get_reference_value_by_name(self, 'nestedConcern')
        return [(cast(ConcernUsage, r.referred) if r else None) for r in res]

    def add_to_nested_concern(self, new_element: 'ConcernUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedConcern'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedCase(self) ->List['CaseUsage']:
        res = get_reference_value_by_name(self, 'nestedCase')
        return [(cast(CaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_case(self, new_element: 'CaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedAnalysisCase(self) ->List['AnalysisCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedAnalysisCase')
        return [(cast(AnalysisCaseUsage, r.referred) if r else None) for r in
            res]

    def add_to_nested_analysis_case(self, new_element: 'AnalysisCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedAnalysisCase'), ReferenceValue
            (new_element, new_element.name))

    @property
    def nestedVerificationCase(self) ->List['VerificationCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedVerificationCase')
        return [(cast(VerificationCaseUsage, r.referred) if r else None) for
            r in res]

    def add_to_nested_verification_case(self, new_element:
        'VerificationCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedVerificationCase'),
            ReferenceValue(new_element, new_element.name))

    @property
    def nestedUseCase(self) ->List['UseCaseUsage']:
        res = get_reference_value_by_name(self, 'nestedUseCase')
        return [(cast(UseCaseUsage, r.referred) if r else None) for r in res]

    def add_to_nested_use_case(self, new_element: 'UseCaseUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedUseCase'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedView(self) ->List['ViewUsage']:
        res = get_reference_value_by_name(self, 'nestedView')
        return [(cast(ViewUsage, r.referred) if r else None) for r in res]

    def add_to_nested_view(self, new_element: 'ViewUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedView'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedViewpoint(self) ->List['ViewpointUsage']:
        res = get_reference_value_by_name(self, 'nestedViewpoint')
        return [(cast(ViewpointUsage, r.referred) if r else None) for r in res]

    def add_to_nested_viewpoint(self, new_element: 'ViewpointUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedViewpoint'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedRendering(self) ->List['RenderingUsage']:
        res = get_reference_value_by_name(self, 'nestedRendering')
        return [(cast(RenderingUsage, r.referred) if r else None) for r in res]

    def add_to_nested_rendering(self, new_element: 'RenderingUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedRendering'), ReferenceValue(
            new_element, new_element.name))

    @property
    def nestedMetadata(self) ->List['MetadataUsage']:
        res = get_reference_value_by_name(self, 'nestedMetadata')
        return [(cast(MetadataUsage, r.referred) if r else None) for r in res]

    def add_to_nested_metadata(self, new_element: 'MetadataUsage'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('nestedMetadata'), ReferenceValue(
            new_element, new_element.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class BooleanExpression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_boolean_expression()

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Expression(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_expression()

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Invariant(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_invariant()

    @property
    def isNegated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNegated'))

    @isNegated.setter
    def isNegated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isNegated')
        self.set_property_value(property=property_, value=value)

    @property
    def predicate(self) ->Optional['IPredicate']:
        res = get_only_reference_value_by_reference_name(self, 'predicate')
        if res:
            return cast(IPredicate, res.referred)
        else:
            return None

    @predicate.setter
    def predicate(self, predicate: 'IPredicate'):
        reference = self.get_classifier().get_reference_by_name('predicate')
        if self.predicate:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(predicate,
            predicate.name))

    @property
    def function(self) ->Optional['IFunction']:
        res = get_only_reference_value_by_reference_name(self, 'function')
        if res:
            return cast(IFunction, res.referred)
        else:
            return None

    @function.setter
    def function(self, function: 'IFunction'):
        reference = self.get_classifier().get_reference_by_name('function')
        if self.function:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(function,
            function.name))

    @property
    def result(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'result')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @result.setter
    def result(self, result: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('result')
        if self.result:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(result, result.name)
            )

    @property
    def isModelLevelEvaluable(self) ->bool:
        return cast(bool, get_property_value_by_name(self,
            'isModelLevelEvaluable'))

    @isModelLevelEvaluable.setter
    def isModelLevelEvaluable(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isModelLevelEvaluable')
        self.set_property_value(property=property_, value=value)

    @property
    def behavior(self) ->List['IBehavior']:
        res = get_reference_value_by_name(self, 'behavior')
        return [(cast(IBehavior, r.referred) if r else None) for r in res]

    def add_to_behavior(self, new_element: 'IBehavior'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('behavior'), ReferenceValue(
            new_element, new_element.name))

    @property
    def parameter(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'parameter')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_parameter(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('parameter'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Expose(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_expose()

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class Import(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_import()

    @property
    def visibility(self) ->VisibilityKind:
        return cast(VisibilityKind, get_property_value_by_name(self,
            'visibility'))

    @visibility.setter
    def visibility(self, value: VisibilityKind):
        property_ = self.get_classifier().require_property_by_name('visibility'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isRecursive(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isRecursive'))

    @isRecursive.setter
    def isRecursive(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isRecursive')
        self.set_property_value(property=property_, value=value)

    @property
    def isImportAll(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImportAll'))

    @isImportAll.setter
    def isImportAll(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImportAll')
        self.set_property_value(property=property_, value=value)

    @property
    def importedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'importedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @importedElement.setter
    def importedElement(self, importedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'importedElement')
        if self.importedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(importedElement,
            importedElement.name))

    @property
    def importOwningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'importOwningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @importOwningNamespace.setter
    def importOwningNamespace(self, importOwningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'importOwningNamespace')
        if self.importOwningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            importOwningNamespace, importOwningNamespace.name))

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)


class BindingConnector(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_binding_connector()

    @property
    def relatedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'relatedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_related_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def association(self) ->List['IAssociation']:
        res = get_reference_value_by_name(self, 'association')
        return [(cast(IAssociation, r.referred) if r else None) for r in res]

    def add_to_association(self, new_element: 'IAssociation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('association'), ReferenceValue(
            new_element, new_element.name))

    @property
    def connectorEnd(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'connectorEnd')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_connector_end(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('connectorEnd'), ReferenceValue(
            new_element, new_element.name))

    @property
    def sourceFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'sourceFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @sourceFeature.setter
    def sourceFeature(self, sourceFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('sourceFeature'
            )
        if self.sourceFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(sourceFeature,
            sourceFeature.name))

    @property
    def targetFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'targetFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_target_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('targetFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'owningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @owningType.setter
    def owningType(self, owningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('owningType')
        if self.owningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningType,
            owningType.name))

    @property
    def isUnique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isUnique'))

    @isUnique.setter
    def isUnique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isUnique')
        self.set_property_value(property=property_, value=value)

    @property
    def isOrdered(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isOrdered'))

    @isOrdered.setter
    def isOrdered(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isOrdered')
        self.set_property_value(property=property_, value=value)

    @property
    def type(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'type')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('type'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedRedefinition(self) ->List['Redefinition']:
        res = get_reference_value_by_name(self, 'ownedRedefinition')
        return [(cast(Redefinition, r.referred) if r else None) for r in res]

    def add_to_owned_redefinition(self, new_element: 'Redefinition'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedRedefinition'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSubsetting(self) ->List['Subsetting']:
        res = get_reference_value_by_name(self, 'ownedSubsetting')
        return [(cast(Subsetting, r.referred) if r else None) for r in res]

    def add_to_owned_subsetting(self, new_element: 'Subsetting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubsetting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningFeatureMembership(self) ->Optional['FeatureMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningFeatureMembership')
        if res:
            return cast(FeatureMembership, res.referred)
        else:
            return None

    @owningFeatureMembership.setter
    def owningFeatureMembership(self, owningFeatureMembership:
        'FeatureMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningFeatureMembership')
        if self.owningFeatureMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningFeatureMembership, owningFeatureMembership.name))

    @property
    def isComposite(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isComposite'))

    @isComposite.setter
    def isComposite(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isComposite')
        self.set_property_value(property=property_, value=value)

    @property
    def isEnd(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isEnd'))

    @isEnd.setter
    def isEnd(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isEnd')
        self.set_property_value(property=property_, value=value)

    @property
    def endOwningType(self) ->Optional['IType']:
        res = get_only_reference_value_by_reference_name(self, 'endOwningType')
        if res:
            return cast(IType, res.referred)
        else:
            return None

    @endOwningType.setter
    def endOwningType(self, endOwningType: 'IType'):
        reference = self.get_classifier().get_reference_by_name('endOwningType'
            )
        if self.endOwningType:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(endOwningType,
            endOwningType.name))

    @property
    def ownedTyping(self) ->List['FeatureTyping']:
        res = get_reference_value_by_name(self, 'ownedTyping')
        return [(cast(FeatureTyping, r.referred) if r else None) for r in res]

    def add_to_owned_typing(self, new_element: 'FeatureTyping'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTyping'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featuringType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'featuringType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_featuring_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featuringType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedTypeFeaturing(self) ->List['TypeFeaturing']:
        res = get_reference_value_by_name(self, 'ownedTypeFeaturing')
        return [(cast(TypeFeaturing, r.referred) if r else None) for r in res]

    def add_to_owned_type_featuring(self, new_element: 'TypeFeaturing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedTypeFeaturing'), ReferenceValue
            (new_element, new_element.name))

    @property
    def isDerived(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isDerived'))

    @isDerived.setter
    def isDerived(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isDerived')
        self.set_property_value(property=property_, value=value)

    @property
    def chainingFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'chainingFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_chaining_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('chainingFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedFeatureInverting(self) ->List['FeatureInverting']:
        res = get_reference_value_by_name(self, 'ownedFeatureInverting')
        return [(cast(FeatureInverting, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_inverting(self, new_element: 'FeatureInverting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureInverting'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureChaining(self) ->List['FeatureChaining']:
        res = get_reference_value_by_name(self, 'ownedFeatureChaining')
        return [(cast(FeatureChaining, r.referred) if r else None) for r in res
            ]

    def add_to_owned_feature_chaining(self, new_element: 'FeatureChaining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureChaining'),
            ReferenceValue(new_element, new_element.name))

    @property
    def isReadOnly(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isReadOnly'))

    @isReadOnly.setter
    def isReadOnly(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isReadOnly'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def isPortion(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isPortion'))

    @isPortion.setter
    def isPortion(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isPortion')
        self.set_property_value(property=property_, value=value)

    @property
    def direction(self) ->FeatureDirectionKind:
        return cast(FeatureDirectionKind, get_property_value_by_name(self,
            'direction'))

    @direction.setter
    def direction(self, value: FeatureDirectionKind):
        property_ = self.get_classifier().require_property_by_name('direction')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedReferenceSubsetting(self) ->Optional['ReferenceSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedReferenceSubsetting')
        if res:
            return cast(ReferenceSubsetting, res.referred)
        else:
            return None

    @ownedReferenceSubsetting.setter
    def ownedReferenceSubsetting(self, ownedReferenceSubsetting:
        'ReferenceSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedReferenceSubsetting')
        if self.ownedReferenceSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedReferenceSubsetting, ownedReferenceSubsetting.name))

    @property
    def crossFeature(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'crossFeature')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @crossFeature.setter
    def crossFeature(self, crossFeature: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('crossFeature')
        if self.crossFeature:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(crossFeature,
            crossFeature.name))

    @property
    def ownedCrossSubsetting(self) ->Optional['CrossSubsetting']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedCrossSubsetting')
        if res:
            return cast(CrossSubsetting, res.referred)
        else:
            return None

    @ownedCrossSubsetting.setter
    def ownedCrossSubsetting(self, ownedCrossSubsetting: 'CrossSubsetting'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedCrossSubsetting')
        if self.ownedCrossSubsetting:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            ownedCrossSubsetting, ownedCrossSubsetting.name))

    @property
    def featureTarget(self) ->Optional['IFeature']:
        res = get_only_reference_value_by_reference_name(self, 'featureTarget')
        if res:
            return cast(IFeature, res.referred)
        else:
            return None

    @featureTarget.setter
    def featureTarget(self, featureTarget: 'IFeature'):
        reference = self.get_classifier().get_reference_by_name('featureTarget'
            )
        if self.featureTarget:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(featureTarget,
            featureTarget.name))

    @property
    def isNonunique(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isNonunique'))

    @isNonunique.setter
    def isNonunique(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isNonunique')
        self.set_property_value(property=property_, value=value)

    @property
    def owningRelatedElement(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelatedElement')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owningRelatedElement.setter
    def owningRelatedElement(self, owningRelatedElement: 'IElement'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelatedElement')
        if self.owningRelatedElement:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelatedElement, owningRelatedElement.name))

    @property
    def relatedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'relatedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_related_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('relatedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def target(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'target')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_target(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('target'), ReferenceValue(new_element,
            new_element.name))

    @property
    def source(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'source')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_source(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('source'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isImplied(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImplied'))

    @isImplied.setter
    def isImplied(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isImplied')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))


class Metaclass(DynamicNode):

    def __init__(self, id: str):
        super().__init__(id)
        self.concept = get_metaclass()

    @property
    def ownedSubclassification(self) ->List['Subclassification']:
        res = get_reference_value_by_name(self, 'ownedSubclassification')
        return [(cast(Subclassification, r.referred) if r else None) for r in
            res]

    def add_to_owned_subclassification(self, new_element: 'Subclassification'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSubclassification'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeatureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'ownedFeatureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_owned_feature_membership(self, new_element: 'FeatureMembership'
        ):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeatureMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def ownedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedEndFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'ownedEndFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_owned_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedEndFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def feature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'feature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('feature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def input(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'input')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_input(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('input'), ReferenceValue(new_element,
            new_element.name))

    @property
    def output(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'output')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_output(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('output'), ReferenceValue(new_element,
            new_element.name))

    @property
    def isAbstract(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isAbstract'))

    @isAbstract.setter
    def isAbstract(self, value: bool):
        property_ = self.get_classifier().require_property_by_name('isAbstract'
            )
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'inheritedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_inherited_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedMembership'),
            ReferenceValue(new_element, new_element.name))

    @property
    def endFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'endFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_end_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('endFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def isSufficient(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isSufficient'))

    @isSufficient.setter
    def isSufficient(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isSufficient')
        self.set_property_value(property=property_, value=value)

    @property
    def ownedConjugator(self) ->Optional['Conjugation']:
        res = get_only_reference_value_by_reference_name(self,
            'ownedConjugator')
        if res:
            return cast(Conjugation, res.referred)
        else:
            return None

    @ownedConjugator.setter
    def ownedConjugator(self, ownedConjugator: 'Conjugation'):
        reference = self.get_classifier().get_reference_by_name(
            'ownedConjugator')
        if self.ownedConjugator:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(ownedConjugator,
            ownedConjugator.name))

    @property
    def isConjugated(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isConjugated'))

    @isConjugated.setter
    def isConjugated(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isConjugated')
        self.set_property_value(property=property_, value=value)

    @property
    def inheritedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'inheritedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_inherited_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('inheritedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def multiplicity(self) ->Optional['Multiplicity']:
        res = get_only_reference_value_by_reference_name(self, 'multiplicity')
        if res:
            return cast(Multiplicity, res.referred)
        else:
            return None

    @multiplicity.setter
    def multiplicity(self, multiplicity: 'Multiplicity'):
        reference = self.get_classifier().get_reference_by_name('multiplicity')
        if self.multiplicity:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(multiplicity,
            multiplicity.name))

    @property
    def unioningType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'unioningType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_unioning_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('unioningType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedIntersecting(self) ->List['Intersecting']:
        res = get_reference_value_by_name(self, 'ownedIntersecting')
        return [(cast(Intersecting, r.referred) if r else None) for r in res]

    def add_to_owned_intersecting(self, new_element: 'Intersecting'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedIntersecting'), ReferenceValue(
            new_element, new_element.name))

    @property
    def intersectingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'intersectingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_intersecting_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('intersectingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedUnioning(self) ->List['Unioning']:
        res = get_reference_value_by_name(self, 'ownedUnioning')
        return [(cast(Unioning, r.referred) if r else None) for r in res]

    def add_to_owned_unioning(self, new_element: 'Unioning'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedUnioning'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDisjoining(self) ->List['Disjoining']:
        res = get_reference_value_by_name(self, 'ownedDisjoining')
        return [(cast(Disjoining, r.referred) if r else None) for r in res]

    def add_to_owned_disjoining(self, new_element: 'Disjoining'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDisjoining'), ReferenceValue(
            new_element, new_element.name))

    @property
    def featureMembership(self) ->List['FeatureMembership']:
        res = get_reference_value_by_name(self, 'featureMembership')
        return [(cast(FeatureMembership, r.referred) if r else None) for r in
            res]

    def add_to_feature_membership(self, new_element: 'FeatureMembership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('featureMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def differencingType(self) ->List['IType']:
        res = get_reference_value_by_name(self, 'differencingType')
        return [(cast(IType, r.referred) if r else None) for r in res]

    def add_to_differencing_type(self, new_element: 'IType'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('differencingType'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedDifferencing(self) ->List['Differencing']:
        res = get_reference_value_by_name(self, 'ownedDifferencing')
        return [(cast(Differencing, r.referred) if r else None) for r in res]

    def add_to_owned_differencing(self, new_element: 'Differencing'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedDifferencing'), ReferenceValue(
            new_element, new_element.name))

    @property
    def directedFeature(self) ->List['IFeature']:
        res = get_reference_value_by_name(self, 'directedFeature')
        return [(cast(IFeature, r.referred) if r else None) for r in res]

    def add_to_directed_feature(self, new_element: 'IFeature'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('directedFeature'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedSpecialization(self) ->List['Specialization']:
        res = get_reference_value_by_name(self, 'ownedSpecialization')
        return [(cast(Specialization, r.referred) if r else None) for r in res]

    def add_to_owned_specialization(self, new_element: 'Specialization'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedSpecialization'),
            ReferenceValue(new_element, new_element.name))

    @property
    def membership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'membership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('membership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedImport(self) ->List['IImport']:
        res = get_reference_value_by_name(self, 'ownedImport')
        return [(cast(IImport, r.referred) if r else None) for r in res]

    def add_to_owned_import(self, new_element: 'IImport'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedImport'), ReferenceValue(
            new_element, new_element.name))

    @property
    def member(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'member')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('member'), ReferenceValue(new_element,
            new_element.name))

    @property
    def ownedMember(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedMember')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_member(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMember'), ReferenceValue(
            new_element, new_element.name))

    @property
    def importedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'importedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_imported_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('importedMembership'), ReferenceValue
            (new_element, new_element.name))

    @property
    def ownedMembership(self) ->List['Membership']:
        res = get_reference_value_by_name(self, 'ownedMembership')
        return [(cast(Membership, r.referred) if r else None) for r in res]

    def add_to_owned_membership(self, new_element: 'Membership'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedMembership'), ReferenceValue(
            new_element, new_element.name))

    @property
    def owningMembership(self) ->Optional['OwningMembership']:
        res = get_only_reference_value_by_reference_name(self,
            'owningMembership')
        if res:
            return cast(OwningMembership, res.referred)
        else:
            return None

    @owningMembership.setter
    def owningMembership(self, owningMembership: 'OwningMembership'):
        reference = self.get_classifier().get_reference_by_name(
            'owningMembership')
        if self.owningMembership:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningMembership,
            owningMembership.name))

    @property
    def owningNamespace(self) ->Optional['INamespace']:
        res = get_only_reference_value_by_reference_name(self,
            'owningNamespace')
        if res:
            return cast(INamespace, res.referred)
        else:
            return None

    @owningNamespace.setter
    def owningNamespace(self, owningNamespace: 'INamespace'):
        reference = self.get_classifier().get_reference_by_name(
            'owningNamespace')
        if self.owningNamespace:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owningNamespace,
            owningNamespace.name))

    @property
    def owningRelationship(self) ->Optional['IRelationship']:
        res = get_only_reference_value_by_reference_name(self,
            'owningRelationship')
        if res:
            return cast(IRelationship, res.referred)
        else:
            return None

    @owningRelationship.setter
    def owningRelationship(self, owningRelationship: 'IRelationship'):
        reference = self.get_classifier().get_reference_by_name(
            'owningRelationship')
        if self.owningRelationship:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(
            owningRelationship, owningRelationship.name))

    @property
    def elementId(self) ->str:
        return cast(str, get_property_value_by_name(self, 'elementId'))

    @elementId.setter
    def elementId(self, value: str):
        property_ = self.get_classifier().require_property_by_name('elementId')
        self.set_property_value(property=property_, value=value)

    @property
    def owner(self) ->Optional['IElement']:
        res = get_only_reference_value_by_reference_name(self, 'owner')
        if res:
            return cast(IElement, res.referred)
        else:
            return None

    @owner.setter
    def owner(self, owner: 'IElement'):
        reference = self.get_classifier().get_reference_by_name('owner')
        if self.owner:
            self.remove_reference_value_by_index(reference, 0)
        self.add_reference_value(reference, ReferenceValue(owner, owner.name))

    @property
    def ownedElement(self) ->List['IElement']:
        res = get_reference_value_by_name(self, 'ownedElement')
        return [(cast(IElement, r.referred) if r else None) for r in res]

    def add_to_owned_element(self, new_element: 'IElement'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedElement'), ReferenceValue(
            new_element, new_element.name))

    @property
    def documentation(self) ->List['Documentation']:
        res = get_reference_value_by_name(self, 'documentation')
        return [(cast(Documentation, r.referred) if r else None) for r in res]

    def add_to_documentation(self, new_element: 'Documentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('documentation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def ownedAnnotation(self) ->List['Annotation']:
        res = get_reference_value_by_name(self, 'ownedAnnotation')
        return [(cast(Annotation, r.referred) if r else None) for r in res]

    def add_to_owned_annotation(self, new_element: 'Annotation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('ownedAnnotation'), ReferenceValue(
            new_element, new_element.name))

    @property
    def textualRepresentation(self) ->List['TextualRepresentation']:
        res = get_reference_value_by_name(self, 'textualRepresentation')
        return [(cast(TextualRepresentation, r.referred) if r else None) for
            r in res]

    def add_to_textual_representation(self, new_element:
        'TextualRepresentation'):
        self.add_reference_value(self.get_classifier().
            require_reference_by_name('textualRepresentation'),
            ReferenceValue(new_element, new_element.name))

    @property
    def declaredShortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredShortName'))

    @declaredShortName.setter
    def declaredShortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredShortName')
        self.set_property_value(property=property_, value=value)

    @property
    def declaredName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'declaredName'))

    @declaredName.setter
    def declaredName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'declaredName')
        self.set_property_value(property=property_, value=value)

    @property
    def shortName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'shortName'))

    @shortName.setter
    def shortName(self, value: str):
        property_ = self.get_classifier().require_property_by_name('shortName')
        self.set_property_value(property=property_, value=value)

    @property
    def name(self) ->str:
        return cast(str, get_property_value_by_name(self, 'name'))

    @name.setter
    def name(self, value: str):
        property_ = self.get_classifier().require_property_by_name('name')
        self.set_property_value(property=property_, value=value)

    @property
    def qualifiedName(self) ->str:
        return cast(str, get_property_value_by_name(self, 'qualifiedName'))

    @qualifiedName.setter
    def qualifiedName(self, value: str):
        property_ = self.get_classifier().require_property_by_name(
            'qualifiedName')
        self.set_property_value(property=property_, value=value)

    @property
    def isImpliedIncluded(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isImpliedIncluded')
            )

    @isImpliedIncluded.setter
    def isImpliedIncluded(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isImpliedIncluded')
        self.set_property_value(property=property_, value=value)

    @property
    def isLibraryElement(self) ->bool:
        return cast(bool, get_property_value_by_name(self, 'isLibraryElement'))

    @isLibraryElement.setter
    def isLibraryElement(self, value: bool):
        property_ = self.get_classifier().require_property_by_name(
            'isLibraryElement')
        self.set_property_value(property=property_, value=value)
